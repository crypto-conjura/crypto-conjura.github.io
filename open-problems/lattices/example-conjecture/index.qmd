---
title: "Example Conjecture"
status:
  statement_informal: ai
  statement_formal: open
  statement_match: open
  proof_informal: open
  proof_review: ai
  proof_formal: open
  statement_by: ""
  proof_by: ""
# status_badge is generated by scripts/status_badge.py -- do not hand-edit
categories: [Lattices]
status_badge: "<svg class='cj-status-badge' viewBox='0 0 32 32' width='28' height='28' xmlns='http://www.w3.org/2000/svg' role='img'><title>Statement: AI-written, not yet formalized. Proof: open -- no attempt yet.</title><circle cx='16' cy='16' r='13' fill='none' stroke-width='4' class='cj-ring-0' stroke-dasharray='1,3'/><circle cx='16' cy='16' r='8' class='cj-disc-0'/><text x='16' y='17' text-anchor='middle' dominant-baseline='central' class='cj-glyph' font-size='9'>–</text></svg>"
---

::: {.callout-note appearance="simple"}
This is a template page. Replace this content with the real conjecture, then delete this note. Copy this whole folder (`index.qmd`, `latex/`, `pdf/`, `sessions/`, `lean/`) as the starting point for a new conjecture.
:::

## Status

Set the six `status:` fields above to describe where this conjecture actually stands, then run `python3 scripts/status_badge.py` from the repo root to regenerate the badge below and the `status_badge:` frontmatter value — see the legend on the [Open Problems](/open-problems/index.qmd) page for what the six fields and the badge mean.

<!-- status:start -->
<svg class='cj-status-badge' viewBox='0 0 32 32' width='28' height='28' xmlns='http://www.w3.org/2000/svg' role='img'><title>Statement: AI-written, not yet formalized. Proof: open -- no attempt yet.</title><circle cx='16' cy='16' r='13' fill='none' stroke-width='4' class='cj-ring-0' stroke-dasharray='1,3'/><circle cx='16' cy='16' r='8' class='cj-disc-0'/><text x='16' y='17' text-anchor='middle' dominant-baseline='central' class='cj-glyph' font-size='9'>–</text></svg>

*Statement: AI-written, not yet formalized. Proof: open -- no attempt yet.*
<!-- status:end -->

- *Best known result:* —
- *Related work:* —

::: {.panel-tabset}

## Statement

[View PDF](pdf/main.pdf) · [Download LaTeX source](latex/main.tex)

State the conjecture precisely here, using standard math notation, e.g. $\mathsf{SIS}_{n,m,q,\beta}$ is hard for parameters ...

## Proof

This conjecture is open — no proof exists yet.

Once (an attempt at) a proof exists, add its own `.tex` source and compiled PDF (e.g. `latex/proof.tex` / `pdf/proof.pdf`) and link them here, following the same convention as the statement.

## Formal Artifact

[Lean statement](lean/Statement.lean) — autoformalized by ⟨name the AI model here, e.g. "Claude Sonnet 5"⟩; set `status.statement_by` above to match, and once a human has checked it against the tex statement, raise `status.statement_match` to `human` (or `ai` if an AI checked the match) and regenerate the badge.

Only the conjecture's *statement* has been formalized so far (as a Lean theorem ending in `sorry` — see `lean/Statement.lean`), not a proof. Once a proof exists, replace the `sorry` (or add a separate `lean/Proof.lean`), set `status.proof_informal`/`status.proof_review`/`status.proof_formal`/`status.proof_by` to match, and regenerate the badge.

:::

## Source material

If this conjecture was imported from an existing paper, the original LaTeX source and compiled PDF live alongside this page in `latex/` and `pdf/` (see the Statement tab above).

## Research sessions

Dumps of prompt/agent sessions attempting this conjecture are kept in `sessions/` for provenance. They are not rendered as part of the site.
