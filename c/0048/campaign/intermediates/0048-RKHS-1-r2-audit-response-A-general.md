MODEL: sonnet

### VERDICT ###
STATUS: DEFECTS
The Theorem, Lemmas 1–4, and Corollary correctly and completely resolve Question 8.1 as stated in the Contract (with an even stronger result: exact equality and an O(log n) entropy bound), but Remark C.1 contains a false quantitative claim (the asymptotic measure of the "negative region" is asserted to be 1−o(1) when it is in fact Θ(1) ≈ 0.68), which is a genuine class-B error even though it does not touch the load-bearing proof.

### FINDINGS ###

| quoted location | class A-E | explanation |
|---|---|---|
| "Claim: for every `ε > 0` and every `δ > 0` ... `C(ε,δ) = 2/(eδ)` — no dependence on `ε`" (Goal / Corollary) | — (no defect) | Verified: this exactly matches the Contract's statement and reading conventions (constant may but need not depend on ε; must not depend on n, μ). The construction gives error exactly 0, so it satisfies `≤ ε‖L‖_F` for every ε>0 with the same C(ε,δ); no drift. |
| Lemma 1(a): "`d ≤ n(n+1)/2` and `‖q_Q‖_∞ ≤ ‖Q‖_F`" | — (no defect) | Checked: `Q ↦ q_Q` restricted to the sphere is injective on `Sym_n` (v^TMv=0 for all unit v forces symmetric M=0), so this bound is correct (and is attained with equality for measures of full support, e.g. uniform μ, as I verified directly in the n=2 numerical example and again for uniform sphere measure). `‖Q‖_op ≤ ‖Q‖_F` is standard. |
| Lemma 1(b), the open-set/support argument | — (no defect) | Re-derived independently: `U:={|h|>|h(v0)|/2}` is open, contains `v0∈supp μ`, hence has positive measure by definition of support, yet `U⊆{h≠0}` is null — valid contradiction. Correct use of continuity of quadratic forms. |
| Lemma 1(c), Riesz representer | — (no defect) | Standard finite-dimensional Hilbert space fact, correctly invoked (F_μ is finite-dimensional, hence complete). |
| Lemma 2(i)–(iii) | — (no defect) | Recomputed each identity from the reproducing property and Cauchy–Schwarz; all correct, including `E_μ[k]=1`, `k≠0`, and `1≤E_μ|k|≤K(v0,v0)^{1/2}`. |
| Lemma 3, "`∫K(v,v)dμ = d`" and existence of a cheap `v0`" | — (no defect) | Verified the Parseval step `K(v,v)=Σ_j q_j(v)²` for `v∈supp μ`, the integral identity (3), and the "positive-measure of the below-average set" argument (a standard averaging trick, correctly executed with the null-complement fact P1). |
| Lemma 4, entropy ≤ log second moment | — (no defect) | Re-derived: `φ(t)=t log t ≤ t²` for `t≥0` (via `log t≤t-1≤t` for `t≥1`, and `φ≤0` for `t<1`) and the Jensen argument under `dν=|r|dμ` are both correct; the handling of `0 log 0 := 0` versus the ν-null set `{r=0}` is correctly matched. |
| Proof of the Theorem (construction of `c`, `r`, `L`) | — (no defect) | Recomputed end-to-end: `c=1/E_μ|k| ∈ [K(v0,v0)^{-1/2},1]`, `E_μ|r|=1`, `E_μ[r vv^T]=c·v0v0^T` exactly (error 0), and the entropy chain `log E_μ[r²] = log(K(v0,v0)/(E_μ|k|)²) ≤ log K(v0,v0) ≤ log(n(n+1)/2)`. All steps check out arithmetically and logically. I also independently verified the n=1 edge case (point mass) by hand: forces c=1, entropy 0, matching the artifact's own n=1 remark in the Corollary proof. |
| Corollary, "`max_{x>0}(log x)/x^δ = 1/(eδ)`" and the chain `log(n(n+1)/2)≤2log n≤(2/(eδ))n^δ` | — (no defect) | Recomputed the calculus maximization (critical point at `x=e^{1/δ}`, value `1/(eδ)`) and the elementary inequality `n(n+1)/2≤n²` for `n≥1`; both correct, including the `n=1` boundary case (`log 1 = 0`). |
| Remark A | — (no defect, and correctly labeled "not load-bearing") | Re-derived the quadratic-in-t discriminant condition and root-positivity argument from scratch; the stated iff-condition `Σ_{i≥2}λ_i² ≤ (ε²/(1−ε²))λ_1²` is correct. |
| Remark C.1: "the kernel at any `v0` is `k(v) = (n(n+2)/2)⟨v,v0⟩² − n/2`" | — (no defect) | Independently verified via the exact 4th-moment formula for the uniform sphere measure (`E[v_iv_jv_av_b] = (δ_{ij}δ_{ab}+δ_{ia}δ_{jb}+δ_{ib}δ_{ja})/(n(n+2))`) that this k satisfies both `E_μ[k]=1` and `E_μ[k vv^T]=v0v0^T` exactly. This part of the remark is correct. |
| Remark C.1: "negative on `{⟨v,v0⟩² < 1/(n+2)}`, a set of `μ`-measure `1 − o(1)`" | **(B) CRITICAL ERROR** | This is false. Writing `t=⟨v,v0⟩`, `t²` is exactly `Beta(1/2,(n-1)/2)`-distributed under uniform μ. As `n→∞`, `n·t² → χ²_1` in distribution (standard fact: projections of the uniform sphere measure become asymptotically Gaussian, `√n·t → N(0,1)`), and the threshold `n/(n+2)→1`, so `P(t² < 1/(n+2)) → P(χ²_1 < 1) = 2Φ(1)-1 ≈ 0.6827`, a constant strictly less than 1 — **not** `1-o(1)`. (I checked this both via the asymptotic argument and by direct exact computation for `n=2`: on the circle, `P(cos²θ<1/4)=1/3`, consistent with convergence toward ≈0.68 from below, never toward 1.) The qualitative point the remark is making — that `k` is negative on a set of positive measure, hence genuinely signed on this "hard" `μ`, hence no contradiction with the paper's nonnegative-reweighting tightness claim — survives (0.68 > 0 for all large `n`), so this error is **not load-bearing** for the Theorem, Corollary, or even for the surrounding qualitative argument of Remark C.1. But it is a definite false quantitative statement asserted as fact, and per the review brief a false claim in a remark is still a false claim. |
| Remark C.2 (flatness dictionary not satisfied by the construction) | — (no defect) | Checked against card Q4/Q9: correctly identifies that the paper's submatrix dictionary requires a *flat* reweighting, that the Theorem's `r` carries no flatness guarantee, and correctly declines to claim the log-rank consequence. Appropriately conservative. |
| Remark C.3 (pseudo-distribution obstruction) | — (no defect, informal/scope commentary, explicitly hedged) | The claim that `ev_{v0}` (support-point evaluation) has no pseudo-distribution analogue while the degree-4 moment inner product does is a reasonable and correctly-scoped observation; it is presented as diagnostic commentary, not as a proven result, and is consistent with the source card's own framing ("if appropriately extended to pseudo-distributions"). |
| Corollary B, "Run Lemmas 1–4 verbatim with `S^{n-1}` replaced by `X`... and the space of quadratic forms replaced by [the affine functions `G`]" | (C) JUSTIFICATION GAP, routine | The adaptation is plausible and I checked the two ingredients it claims are sufficient (constant `1 ∈ G`, coordinate functions `X↦X_ab ∈ G`, `dim G ≤ n²+1`, compactness of `X` giving boundedness), all of which hold. But the sketch does not spell out that Lemma 3's Parseval/"cheap-point" argument and Lemma 1(b)'s a.e.-to-everywhere-on-support argument go through unchanged for an *affine* (rather than *linear*) parametrization — this is true (the set `G` itself, as a set of functions, is a linear subspace of `L²(μ)` even though individual elements are described by an affine parametrization `(α,A)`), but a reader must verify this rather than take "verbatim" at face value. I judge this a routine gap a competent reader fills without effort; it does not by itself force DEFECTS. Corollary B is not part of the Contract's required statement in any case (it is offered as supplementary material). |
| Citations to the source paper (Question 8.1, Theorem 2.3, Definition 2.2/Q9, §2.2/Q4, p.6 tightness/Q5, footnotes 7–8/Q6–Q7, §2.3/Q8) | — (no defect) | Cross-checked every verbatim quotation in the artifact's DEPENDENCIES section and in Remark C word-for-word against the corresponding card items Q1–Q9; all match exactly (allowing for the artifact quoting a subset of a longer card passage, never a misquote). Definition 2.2's "deficient ⇒ nonnegative" gloss used in Remark C.1 is exactly supported by the card's own note on Q9. No citation defect, no unverifiable citation. |
| Completeness | — (no defect) | Artifact ends with `### END OF ARTIFACT [P-1] ###`; not truncated. |

### STEP LOG ###
- Step 0 completeness check: artifact terminates properly with its END OF ARTIFACT line — PASS, not truncated.
- Statement-drift check: Contract's Question 8.1 (asymptotic `O(n^δ)` entropy, relative Frobenius error `≤ε‖L‖_F`, nonzero rank-one `L`, signed reweighting per the shared Definition) is exactly what the artifact proves, at a strictly stronger level of precision (explicit `C(ε,δ)`, exact equality instead of just `≤ε`) — no drift.
- Lemma 1(a),(b),(c),(d): accepted, each re-derived independently as above.
- Lemma 2(i),(ii),(iii): accepted, each re-derived independently.
- Lemma 3: accepted, Parseval identity and averaging argument re-derived independently.
- Lemma 4: accepted, Jensen/φ-bound argument re-derived independently.
- Theorem proof (construction of c, r, L; exact-equality claim; entropy bound): accepted, fully recomputed.
- Corollary (constant `C(ε,δ)=2/(eδ)`): accepted, calculus maximization and inequality chain recomputed, including n=1 boundary case.
- Remark A: accepted, quadratic-in-t argument recomputed independently.
- Remark B / Corollary B: accepted with a routine, non-blocking justification gap (class C) noted above.
- Remark C.1, kernel formula for uniform sphere: accepted, verified via exact 4th-moment computation.
- Remark C.1, "measure 1-o(1)" claim: REJECTED — recomputed via Beta/χ² asymptotics (limit ≈0.6827) and via an exact n=2 hand computation (1/3); flagged as class B, non-load-bearing.
- Remark C.2, C.3: accepted as appropriately scoped and non-overclaiming commentary.
- Citation audit: all nine quoted passages checked verbatim against the supplied card S1; all match; no (D) or (E) defects.

### SOURCE REQUEST ###
none
