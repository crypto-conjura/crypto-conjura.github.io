---
id: "0007"
problem: three-way-collision-bounded-memory
draft: three-way-collision
title: "Sharp Oblivious Sequential Time-Memory Curve for Finding a 3-Way Collision"
short_title: "3-way collision, oblivious sequential curve"
areas: [symmetric-key]
topics: [collision-finding, time-space-tradeoffs, random-oracle-model]
model: rom
form: tight-bound
assumption_class: unconditional
category: research-open
revision: 1
status:
  statement_informal: ai
  statement_formal: open
  statement_match: open
  proof_informal: open
  proof_review: ai
  proof_formal: open
# status_badge is generated by scripts/status_badge.py -- do not hand-edit
difficulty:
  reach: adaptation
  by: ai
  note: "Both ends are in hand, a proved oblivious lower bound and a table-and-hunt algorithm, and what remains is closing a factor of root-S between them rather than finding a technique."

status_summary: "Open: closing the factor-root-S gap between the proved oblivious lower bound and a matching table-and-hunt algorithm for finding a 3-way collision with an oblivious sequential branching program."
status_updated: 2026-08-19
relations: []
lean:
  mode: none
sources:
  - {cite: "Suzuki, Tonien, Kurosawa and Toyota, Birthday Paradox for Multi-collisions, ICISC 2006", url: "https://link.springer.com/chapter/10.1007/11927587_5"}
  - {cite: "Borodin and Cook, A Time-Space Tradeoff for Sorting on a General Sequential Model of Computation, SIAM J. Comput. 11(2), 1982", url: "https://www.cs.toronto.edu/~sacook/homepage/borodin-cook.pdf"}
  - {cite: "Dinur, Tight Time-Space Lower Bounds for Finding Multiple Collision Pairs and Their Applications, EUROCRYPT 2020", url: "https://eprint.iacr.org/2020/229"}
  - {cite: "Pollard, A Monte Carlo Method for Factorization, BIT 15, 1975", url: "https://link.springer.com/article/10.1007/BF01933667"}
  - {cite: "Brent, An Improved Monte Carlo Factorization Algorithm, BIT 20, 1980", url: "https://link.springer.com/article/10.1007/BF01933190"}
statement_sha: "a1d041c66c5494b55a90f8c41180dd563489e11a696618f4ce13d4d75677971f"
status_badge: "<a href='https://crypto-conjura.github.io/open-problems/status-legend/index.html#status-legend' class='cj-status-link' aria-label='Statement: AI-written, not yet formalized. Proof: open -- no attempt yet. Click for the status badge legend.'><svg class='cj-status-badge' viewBox='0 0 32 32' width='28' height='28' xmlns='http://www.w3.org/2000/svg' role='img'><circle cx='16' cy='16' r='13' fill='none' stroke-width='4' class='cj-ring-0' stroke-dasharray='1,3'/><circle cx='16' cy='16' r='8' class='cj-disc-0'/><text x='16' y='17' text-anchor='middle' dominant-baseline='central' class='cj-glyph' font-size='9'>–</text></svg></a>"
---

<!-- status:start -->
<a href='https://crypto-conjura.github.io/open-problems/status-legend/index.html#status-legend' class='cj-status-link' aria-label='Statement: AI-written, not yet formalized. Proof: open -- no attempt yet. Click for the status badge legend.'><svg class='cj-status-badge' viewBox='0 0 32 32' width='28' height='28' xmlns='http://www.w3.org/2000/svg' role='img'><circle cx='16' cy='16' r='13' fill='none' stroke-width='4' class='cj-ring-0' stroke-dasharray='1,3'/><circle cx='16' cy='16' r='8' class='cj-disc-0'/><text x='16' y='17' text-anchor='middle' dominant-baseline='central' class='cj-glyph' font-size='9'>–</text></svg></a>

*Statement: AI-written, not yet formalized. Proof: open -- no attempt yet.*
<!-- status:end -->

<!-- topics:start -->
<div class="cj-tags cj-page-topics"><a class="cj-tag cj-tag-topic" href="/problems/by-topic/collision-finding/">Collision Finding</a><a class="cj-tag cj-tag-topic" href="/problems/by-topic/random-oracle-model/">Random Oracle Model</a><a class="cj-tag cj-tag-topic" href="/problems/by-topic/time-space-tradeoffs/">Time Space Tradeoffs</a></div>
<!-- topics:end -->

::: {.panel-tabset}

## Statement

[View PDF](pdf/main.pdf) · [LaTeX source](latex/main.tex) · Formal statement — *not yet formalized*

### Setting

Throughout, $N \geq 2$ and $f\colon [N] \to [N]$ is a uniformly random function, available only through an oracle answering $x \mapsto f(x)$. A *$k$-collision* is a set of $k$ distinct points of $[N]$ with a common image under $f$; a *3-collision* is the case $k = 3$. Asymptotic notation $\tilde O, \tilde\Omega, \tilde\Theta$ suppresses factors polylogarithmic in $N$. Success probabilities are over the choice of $f$ and any coins of the algorithm.

A *branching program* -- the model of Borodin and Cook, specialised to a random-function oracle -- queries one point of $[N]$ at a time, with its state between queries capped at $S$ bits; the state may encode anything, so one cannot say what a program *knows*. It is *oblivious* if its query schedule is fixed in advance, independent of the answers seen so far, and *repetition-free* if no point is queried twice.

::: {.callout-note title="Definition (branching program)"}
A branching program of length $T$ and width $W$ is a DAG on levels $0,\dots,T$ with at most $W$ nodes per level, in which every node below level $T$ carries a label $x \in [N]$ and has one outgoing edge for each $y \in [N]$, and every node at level $T$ carries a label in $[N]^3$. It is run by starting at the source, querying the label of the current node, following the edge labelled by the answer, and reading the label of the sink reached; it succeeds if that label is a $3$-collision. Its space is $S = \log_2 W$, counted in bits. It is oblivious if all nodes at a level carry the same label, so that the query schedule $w_1,\dots,w_T$ is fixed in advance, and repetition-free if no point labels two levels.
:::

Two facts fix the landscape. The query threshold is $N^{2/3}$: among $q$ queried points the expected number of $3$-collisions is $\binom{q}{3}N^{-2}$, so $q = \Theta(N^{2/3})$ queries are necessary and sufficient for constant success probability, for *any* algorithm at *any* memory bound (Suzuki, Tonien, Kurosawa and Toyota, ICISC 2006). And the oblivious base case is proved: every repetition-free oblivious branching program succeeding with probability at least $1/2$ satisfies $T \cdot (S + \log_2 N) = \tilde\Omega(N)$, via the multi-pass streaming switching lemma of Dinur (EUROCRYPT 2020) applied to the answer stream. This is the only lower bound known at $k=3$ that mentions memory at all, and it is tight only at $S$ polylogarithmic; for *adaptive* programs nothing memory-sensitive is known.

### The Conjecture

::: {.callout-important title="Conjecture (the oblivious sequential curve at k=3)"}
Every repetition-free oblivious branching program succeeding with probability at least $1/2$ satisfies
$$
T \;=\; \tilde\Omega\bigl(\max\{\, N^{2/3},\; N/\sqrt{S} \,\}\bigr),
$$
equivalently $T \cdot S^{1/2} = \tilde\Omega(N)$ for $S \le N^{2/3}$.
:::

The first clause carries no content; all of it is in $T \cdot S^{1/2}$. The bound is tight: an oblivious program can accumulate $P \le S$ collision pairs at cost $\tilde\Theta(PN/S)$ and then hunt a third preimage of one of the $P$ recorded values in $\tilde\Theta(N/P)$ further queries, both phases non-adaptively; balancing at $P = \sqrt S$ gives $T = \tilde\Theta(N/\sqrt S)$. So a factor $\sqrt S$ separates the proved base case from the conjecture inside its own class, and the two clauses cross at $S = N^{2/3}$, where the curve meets the query threshold and the problem closes.

At $k=2$ memory is free: Pollard's rho method with Brent's cycle detection finds a $2$-collision in $\Theta(\sqrt N)$ queries using $O(\log N)$ bits, and that count is optimal even with unbounded memory. Consequently the conjecture is *false* at $k=2$, and no proof of it can be indifferent to the collision order.

### Sources

- [Suzuki, Tonien, Kurosawa and Toyota, Birthday Paradox for Multi-collisions, ICISC 2006](https://link.springer.com/chapter/10.1007/11927587_5)
- [Borodin and Cook, A Time-Space Tradeoff for Sorting on a General Sequential Model of Computation, SIAM J. Comput. 11(2), 1982](https://www.cs.toronto.edu/~sacook/homepage/borodin-cook.pdf)
- [Dinur, Tight Time-Space Lower Bounds for Finding Multiple Collision Pairs and Their Applications, EUROCRYPT 2020](https://eprint.iacr.org/2020/229)
- [Pollard, A Monte Carlo Method for Factorization, BIT 15, 1975](https://link.springer.com/article/10.1007/BF01933667)
- [Brent, An Improved Monte Carlo Factorization Algorithm, BIT 20, 1980](https://link.springer.com/article/10.1007/BF01933190)

## Proof

View PDF — *no proof written yet* · LaTeX source — *no proof written yet* · Formal proof — *not yet formalized*

This conjecture is open -- no proof exists yet.

Once (an attempt at) a proof exists, add its own `.tex` source and compiled PDF (e.g. `latex/proof.tex` / `pdf/proof.pdf`) and link them here, following the same convention as the statement.

**Formal artifact.**

Not yet formalized in Lean.

## Discussion

The question is not whether a time-memory tradeoff exists but whether the *oblivious sequential* curve is sharp at $k = 3$. Two facts bracket it: the query threshold is $\Theta(N^{2/3})$ for any algorithm at any memory bound, so nothing below that is achievable however much space is used; and the Borodin–Cook branching-program model charges only for state between queries, so a lower bound in it says nothing about what a program *knows*, only about what it can carry.

Obliviousness is the hypothesis doing the work. A fixed query schedule is what makes the counting tractable, and it is also what a solver should be most suspicious of, since adaptive algorithms are the ones actually used. See the [problem page](/p/three-way-collision-bounded-memory/).

**Checked against the literature, 2026-08-19.** Searched for progress on the oblivious sequential 3-collision curve; the most promising candidate, Guo, Nan and Yao's "Revisiting Time-Space Tradeoffs in Collision Search and Decision Problems" (ASIACRYPT 2025, eprint 2025/1550), was fetched and its full text read (via PDF extraction, after an earlier fetch attempt in this sweep failed). The text contains no occurrence of "oblivious," "non-adaptive," or "branching program" anywhere; its algorithms and lower-bound reductions are stated for general algorithms ("for any algorithm $A$") built from chain-based, adaptive constructions, and for random functions with $M = N$ its 3-collision search tradeoff is $T \sim C_3 \cdot N/S$ — strictly better than this conjecture's oblivious curve $\tilde\Theta(N/\sqrt S)$ at the same $S$, consistent with the extra power of adaptive queries rather than a resolution of the oblivious-restricted question. No paper found that resolves this; the conjecture remains open.

## Open obligations

- [ ] Attempt a proof closing the factor-$\sqrt S$ gap between the proved oblivious base case and the matching table-and-hunt algorithm; the companion note argues this needs no new proof technique in either direction, and calls it the smallest open quantity in the larger family of related conjectures.
- [ ] Formalize the statement in Lean; not yet attempted.

:::
