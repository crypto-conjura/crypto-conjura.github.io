id: 0023-scout-3
agent: scout
model: claude-fable-5
cycle: 2
status: COMPLETE

# Bounded deciding-source read — arXiv:2601.08727 (KKDWY26) for rung I01

Date: 2026-08-27. Scope: ONE document, ONE question (per task): does
KKDWY26 v2 (Kothari, Kovacs-Deak, Wang, Rain Zimin Yang, *Rational degree
is polynomially related to degree*) contain anything about pairs of
disjoint-support polynomials, distributions over polynomials, influences,
or juntas that transfers to I01 (cross-disjoint low-influence indicator
families) or singleton-PCC beyond the single lemma already carded as
S3a's dependency? Retrieval: abs page fetched, full v2 PDF fetched from
arxiv.org/pdf/2601.08727v2 and ALL 26 pages read as page images. [READ]
throughout unless noted. Resolves 0023-scout-2 §E.

## ANSWER: outcome (i) of scout-2's §E, with two useful footnotes.

The paper contains **no statement that settles or partially settles I01**:
no distributions over polynomials (only over decision trees, Def. 8 p. 9),
no families (every result is per-function or per-pair), no spread, no
influence HYPOTHESIS in any theorem, and its only influence/junta
CONCLUSIONS are in the exponential regime ($2^{-2\,\mathrm{rdeg}}$ per
relevant coordinate; $O(d^42^{2d})$-variable dependence), i.e. the regime
already covered by card S1 (ACC22 Thm 4.4), not I01's window
$[2^{-d}/d,\,1/(2d))$. **Scout-2's verdict A (NO PRIOR RESOLUTION FOUND)
stands unchanged.** New card: S5 (`../sources/S5-kkdwy26-card.md`).

Point-by-point on the task's checklist:

1. **Theorem 4** [READ, p. 7]: $\mathrm{D}(f)\le 4\deg_\pm(f)^2
   \mathrm{rdeg}(f)^2\le 16\,\mathrm{rdeg}(f)^4$ for every TOTAL Boolean
   $f$; no $n$-dependence. This is what 2608.03824 cites. It does NOT
   apply to the partial (promise) disjoint-support problem — the paper's
   own Fact 7 + footnote 7 (p. 18) show the total-function machinery
   fails for partial functions — so S3a's Lemma 3.5 remains the correct
   promise-setting transfer; T4 adds only the confirming constant
   ($16d^4$, matching S3a's assembled arithmetic) and the proof template
   (hitting sets of maximal monomials, Lemmas 1–3 + Corollary 2).
   The improved Theorem 8 [READ, p. 16], $\mathrm{D}(f)\le
   O(\mathrm{rdeg}^3\log n)$, carries a $\log n$ and is therefore the
   WRONG form for the campaign's $N$-free $\delta$; use T4.

2. **Discrete-Markov lemma** [READ, p. 4]: it is their **Corollary 1**
   (of Markov's inequality, their Theorem 1): $|p|\le h$ on the cube,
   $|p(0^n)|=h$, $p(x)p(0^n)\le 0$ at $|x|=1$ $\Rightarrow$
   $\deg p\ge\sqrt{n/2}$. **Confirms S3a's statement and constant
   exactly** ($h=1$ instance); not sharpened. Statement AND half-page
   proof (symmetrization Fact 5 + Markov) now read at source, so S3a's
   residual [SOURCE-BLOCKED: KKDWY26] on internals is LIFTED — cite card
   S5 item C1. One hypothesis worth flagging for verifiers: (ii) requires
   the value at $0^n$ to ATTAIN the global max modulus of (i).

3. **Disjoint-support / promise-decision results** [READ, pp. 18–19]: one
   pair statement exists — **Theorem 10** (Effective Hypercube
   Nullstellensatz): if $g_1g_2\equiv 0$ on $\{0,1\}^n$ AND $g_1,g_2$
   have NO common zeros, then $h_1g_1+h_2g_2=1$ on the cube with
   multilinearized degrees $\widetilde O(\deg(g_1)^{1.5}\deg(g_2)^{1.5})$.
   Unusable for I01/R5 objects: an incompatible indicator pair has common
   zeros on $(A\cup B)^c$ (the everywhere-promise fails — same flag as
   S3a hyp. 4). Its companion **Fact 7** is genuinely useful as barrier
   context: degree-1 pairs WITHOUT the $g_1g_2\equiv 0$ hypothesis need
   certificate degree $\ge n$ — evidence that any rung proof must exploit
   $fg\equiv 0$ itself, not merely a promise. The $m$-polynomial version
   is their OPEN Conjecture 1; nothing citable for families.

4. **Influence / junta corollaries** [READ, p. 16]: Theorem 9
   ($\mathrm{rdeg}\ge\Omega(\log n)$ for $f$ depending on all $n$
   variables) via Eq. (51): $\mathrm{Inf}_i[f]\ge 2^{-2\,\mathrm{rdeg}(f)}$
   for every relevant $i$ (engine: their Theorem 3 = [NS94, Lemma 2.6],
   $\Pr[p\ne 0]\ge 2^{-\deg p}$); Remark 3: rdeg-$d$ functions depend on
   $O(d^42^{2d})$ variables. All exponential-regime; cannot touch
   $1/\mathrm{poly}(d)$. Their Conjecture 2 (p. 19, Gotsman–Linial
   variant $\mathrm{Inf}[f]\le O(\sqrt n\,\mathrm{ndeg}(f))$) is a
   conjecture, not citable.

Footnote for a later real/complex bridge (S3a flag 1): their Lemma 8
[READ, p. 21] (= [IJK+25, Lemma 26]) — finitely many reals $a_1(x),\dots,
a_N(x)$, at each $x\in D$ some $a_i(x)\ne 0$ $\Rightarrow$ a single
positive combination $\sum c_ia_i$ nonvanishing on all of finite $D$ —
is the tool their App. A uses to reduce complex amplitudes to one real
rational representation; per-point, finite-domain, constants
non-constructive. Recorded in card S5's NOT-say section.

## Card bar

Cleared; card written: `../sources/S5-kkdwy26-card.md` (items C1
[Corollary 1, verbatim + proof — lifts S3a's residual block], T4
[Theorem 4 verbatim + flags], T10+F7 [pair statement + barrier], T9/R3
[influence/junta content, delimiting], plus the does-NOT-contain list).
No change to frozen cards; S3a is superseded only in its residual-block
clause, via S5, append-only.

## E. THE DECIDING SOURCE (this read's residual)

For I01 itself no deciding document remains identified: the deciding
source named by scout-2 is now fully read and is neutral-positive
(outcome (i)). The nearest candidate for a NEW deciding source, if the
rung stalls, would be [IJK+25] (arXiv:2310.08004, *On the Rational Degree
of Boolean Functions*), the only unread paper this one leans on for
lemmas the campaign may reuse (Lemma 26; AND-composition for ndeg) —
low priority, openly on arXiv, read-not-upload.

## F. SOURCE REQUEST

none needed — the deciding source is read in full [READ]; everything
load-bearing is now carded (S1, S2, S3a/S3b, S5); no upload qualifies.

### END OF ARTIFACT 0023-scout-3 ###
