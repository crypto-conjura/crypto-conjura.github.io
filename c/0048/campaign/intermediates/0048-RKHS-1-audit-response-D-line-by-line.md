MODEL: fable

### VERDICT ###
STATUS: DEFECTS
The load-bearing chain (Lemmas 1–4, Theorem, Corollary) is correct and fully proves the Contract statement with C(ε,δ) = 2/(eδ) — every algebraic step, constant, inequality direction and the entire numerical example recompute correctly — but the artifact asserts, twice, a false mathematical claim ("k takes both signs unless μ is a point mass"; "E_μ|k| = K(v_0,v_0)^{1/2} in general") in non-load-bearing commentary, which is a class-B defect on those lines and, under the strict protocol, forces DEFECTS rather than CLEAN.

### FINDINGS ###
| quoted location | class A-E | explanation |
|---|---|---|
| Proof plan: "Cancellation enters through `k`, which takes both signs unless `μ` is a point mass." | B | False as stated. Counterexample: n = 2, μ uniform on {e₁, e₂} ⊂ S¹. Then F_μ is all of R^{supp μ} (values (a,c) of q at the two points), d = 2, and the kernel at v₀ = e₁ is k = (2,0), which is nonnegative although μ is not a point mass. Moreover this v₀ is a legitimate output of Lemma 3 (K(v₀,v₀) = ½·4 = 2 ≤ d = 2), so the Theorem's own construction can produce a nonnegative r for a non-point-mass μ. The correct (and sufficient) statement, made later in the same sentence of Remark C.1, is only that k is negative somewhere whenever E_μ\|k\| > E_μ[k]. Non-load-bearing: no proof step uses signedness of k. |
| Remark C.1: "the kernel `k` has `E_μ[k] = 1` but `E_μ|k| = K(v_0,v_0)^{1/2}` in general" | B | False equation. Cauchy–Schwarz gives only E_μ\|k\| ≤ K(v₀,v₀)^{1/2}, with equality iff \|k\| is μ-a.e. constant on {k ≠ 0}. The artifact's own NUMERICAL CHECK refutes it: there E_μ\|k\| = 3/2 while K(v₀,v₀)^{1/2} = √3 ≈ 1.732. This invalidates the quantitative sentence of Remark C.1 but nothing else: the no-conflict claim with the source's "No for nonnegative r" survives, because the source's negative result is a worst-case-over-μ statement and the construction here only needs to (and does) produce signed r on those worst-case μ. |
| Remark C.1: "Our `r` takes both signs unless `μ` is a point mass" | B | Same false claim as the Proof plan, same counterexample (μ uniform on {e₁,e₂}, r = (2,0) ≥ 0). Confined to the scope remark; the Theorem and Corollary do not depend on it. |

No class A, C, D, or E defects found. The three B-defects above are two occurrences of one false side claim plus one false equation, all in explicitly non-load-bearing commentary (Proof plan prose and Remark C); the proof of the Contract statement is unaffected.

### STEP LOG ###
STEP 0 (completeness): "### END OF ARTIFACT [M-1] ###" present at the last line; no mid-sentence or mid-proof stop. Complete. Proceed.

DRIFT CHECK (class A, first): The artifact proves: for every n ≥ 1 and every Borel probability measure μ on S^{n−1}, there exist v₀ ∈ supp μ, c ∈ [(n(n+1)/2)^{−1/2}, 1], and a signed reweighting r (E_μ|r| = 1, Borel, bounded) with E_μ[r vvᵀ] = c·v₀v₀ᵀ exactly and E_μ[|r| log|r|] ≤ log(n(n+1)/2), hence the Contract statement with C(ε,δ) = 2/(eδ) and Frobenius error 0. Diff against Contract: quantifier order matches (C depends only on δ, a fortiori only on (ε,δ), independent of n and μ); "signed reweighting" definition in the GOAL is verbatim the Contract Definition (E|r| = 1, E[|r| log|r|] ≤ K, 0 log 0 = 0, Borel, μ-integrable, no added regularity — r is bounded Borel, which is allowed since the Contract forbids only *assuming* extra regularity, not providing it); L = c v₀v₀ᵀ is rank one and nonzero (c > 0, ‖v₀‖ = 1); error 0 ≤ ε‖L‖_F for every ε > 0. The artifact proves a strictly stronger statement that implies the Contract. NO DRIFT.

Lemma 1(a): Q ↦ q_Q linear, F_μ = image of Sym_n, so d ≤ dim Sym_n = n(n+1)/2 — recomputed, correct. |vᵀQv| ≤ ‖Q‖_op‖v‖² = ‖Q‖_op ≤ ‖Q‖_F on the sphere — correct (op ≤ F is standard, restated in DEPENDENCIES). L^∞(μ) ⊆ L²(μ) for probability μ — correct. ACCEPTED.
Lemma 1 (P1): complement of supp μ is the union of all open null sets; second countability gives a countable subcover; countable union of null sets is null — correct. (P2) is the definition of support. ACCEPTED.
Lemma 1(b): h := q_{Q−Q'} continuous, μ({h≠0}) = 0; if h(v₀) ≠ 0 for v₀ ∈ supp μ then U = {|h| > |h(v₀)|/2} is open, contains v₀, has μ(U) > 0, yet U ⊆ {h≠0} is null — contradiction. Well-definedness and linearity of ev_v follow. ACCEPTED.
Lemma 1(c): finite-dimensional subspace of L²(μ) is a Hilbert space; every linear functional on it is bounded; Riesz gives unique k_v; k_v = Σⱼ ev_v(fⱼ)fⱼ for an ONB is the standard explicit representer; K(v,v) = ev_v(k_v) by taking f = k_v. ACCEPTED.
Lemma 1(d): q_I(v) = ‖v‖² = 1 on S^{n−1}; q_{E_ab}(v) = vᵀ(e_ae_bᵀ+e_be_aᵀ)v/2 = v_a v_b — recomputed, correct. ACCEPTED. (This is where the sphere constraint is used; the structural note after Lemma 2 is accurate.)
Lemma 2 (integrability preamble): k bounded (1(a)), |v_a v_b| ≤ 1 on the sphere — correct. ACCEPTED.
Lemma 2(i): E_μ[k v_a v_b] = ⟨k, g_ab⟩ = ev_{v₀}(g_ab) = (v₀)_a(v₀)_b for each (a,b) — direct application of 1(c)+1(d). ACCEPTED.
Lemma 2(ii): E_μ[k] = ⟨k,[1]⟩ = ev_{v₀}([1]) = q_I(v₀) = 1; E|k| ≥ |E k| = 1; K = E[k²] ≥ (E|k|)² ≥ 1 by Cauchy–Schwarz against 1 (probability measure — norms of 1 is 1, direction correct); k ≠ 0 since E[k] = 1. ACCEPTED.
Lemma 2(iii): K = ‖k‖² = E[k²] by definition; E|k| ≤ (E k²)^{1/2} = K^{1/2} — Cauchy–Schwarz, direction correct. ACCEPTED. Structural note (trace of (i) gives (ii)): tr E[k vvᵀ] = E[k‖v‖²] = E[k], tr(v₀v₀ᵀ) = 1 — verified, correct.
Lemma 3, identity (2): k_v = Σⱼ ev_v(fⱼ)fⱼ = Σⱼ qⱼ(v)fⱼ for v ∈ supp μ (qⱼ a quadratic-form representative, well-defined at support points by 1(b)); Parseval: K(v,v) = Σⱼ qⱼ(v)² =: g(v) — correct. g polynomial hence Borel; agrees with K(·,·) on supp μ, a set of full measure by (P1), so the integral is well-defined. ACCEPTED.
Lemma 3, identity (3): ∫g dμ = Σⱼ ∫qⱼ² dμ = Σⱼ‖fⱼ‖² = d (each = 1 by orthonormality) — recomputed, correct. ACCEPTED.
Lemma 3, existence: A = {g ≤ d} Borel; if μ(A) = 0 then h = g − d > 0 μ-a.e. with ∫h dμ = 0, forcing h = 0 a.e., contradiction; so μ(A) > 0, and with μ(supp μ) = 1, A ∩ supp μ has positive measure hence is nonempty; any v₀ there has K(v₀,v₀) = g(v₀) ≤ d ≤ n(n+1)/2. ACCEPTED. (Implicitly d ≥ 1 since [1] ∈ F_μ and μ is a probability measure, so an ONB exists — routine.)
Lemma 4, finiteness: φ(t) = t log t has minimum φ(1/e) = −1/e, so φ ≥ −1/e on [0,∞) — recomputed, correct; φ(t) ≤ t² (t ≥ 1: log t ≤ t; t < 1: φ ≤ 0 ≤ t²) — recomputed, correct; hence −1/e ≤ φ(|r|) ≤ r² ∈ L¹, so entropy is a finite real. ACCEPTED.
Lemma 4, Jensen: dν = |r|dμ is a probability measure (E_μ|r| = 1); ν({r=0}) = ∫_{r=0}|r|dμ = 0, so log|r| is ν-a.e. finite and E_ν[log|r|] = E_μ[|r| log|r|], with 0 log 0 := 0 consistent on the ν-null set {r=0}; E_ν[|r|] = E_μ[r²] < ∞; Jensen for concave log: E_ν[log|r|] ≤ log E_ν[|r|] = log E_μ[r²] — all recomputed, directions correct. ACCEPTED. Sharpness note: equality iff |r| ∈ {0,c} a.e.; then E|r| = cμ(S) = 1 gives c = 1/μ(S), E[r²] = c²μ(S) = 1/μ(S), bound = log(1/μ({|r|=c})) — verified, correct (remark only).
Theorem, choice of c: 1 ≤ E|k| ≤ K^{1/2} ≤ d^{1/2} ≤ (n(n+1)/2)^{1/2} gives c = 1/E|k| ∈ [(n(n+1)/2)^{−1/2}, 1] — recomputed, correct. r = c·k with quadratic-form representative q_{cQ_k}: bounded Borel, μ-integrable, E|r| = cE|k| = 1 — admissible per the Definition. ACCEPTED.
Theorem, second moment: E[r vvᵀ] = c v₀v₀ᵀ = L by Lemma 2(i) and linearity (c a scalar); c > 0, ‖v₀v₀ᵀ‖_F = ‖v₀‖² = 1 so ‖L‖_F = c > 0, L = (cv₀)v₀ᵀ rank one, nonzero; error 0 ≤ ε‖L‖_F for every ε > 0. ACCEPTED.
Theorem, entropy: E[|r|log|r|] ≤ log E[r²] = log(c²K) = log(K/(E|k|)²) ≤ log K ≤ log(n(n+1)/2), using E|k| ≥ 1 (so the middle step decreases the argument — direction correct) and Lemma 3 — recomputed, correct. ACCEPTED.
COROLLARY: n = 1: log(1·2/2) = log 1 = 0 ≤ C·1 — correct. n ≥ 2: n(n+1)/2 ≤ n² iff n+1 ≤ 2n iff n ≥ 1 — correct, so log(n(n+1)/2) ≤ 2 log n. Calculus claim: f(x) = (log x)/x^δ has f'(x) = x^{−δ−1}(1 − δ log x), unique critical point at x = e^{1/δ}, f(e^{1/δ}) = (1/δ)/e = 1/(eδ), a maximum (f → −∞ at 0⁺, f → 0 at ∞) — recomputed, the claimed max 1/(eδ) at e^{1/δ} is exactly right. Hence log n ≤ n^δ/(eδ) and 2 log n ≤ (2/(eδ))n^δ. C(ε,δ) = 2/(eδ) is finite for every δ > 0 and independent of n, μ (and ε). ACCEPTED — the Contract statement follows.
REMARK A (not load-bearing, verified anyway): ‖M − tuwᵀ‖² = ‖M‖² − 2t·uᵀMw + t² (recomputed: ⟨M, uwᵀ⟩_F = uᵀMw, ‖uwᵀ‖_F = 1); requirement ⇔ (1−ε²)t² − 2(uᵀMw)t + ‖M‖² ≤ 0; upward parabola (ε < 1), feasible iff discriminant ≥ 0 at the optimal uᵀMw = s₁(M) = |λ₁| (symmetric M); s₁² ≥ (1−ε²)Σλᵢ² ⇔ ε²λ₁² ≥ (1−ε²)Σ_{i≥2}λᵢ² ⇔ Σ_{i≥2}λᵢ² ≤ (ε²/(1−ε²))λ₁² — recomputed, exact. Root product ‖M‖²/(1−ε²) > 0 and sum 2s₁/(1−ε²) > 0 so both roots positive when real — correct. M = 0 case correct. ACCEPTED.
COROLLARY B: G = {X ↦ α + ⟨A,X⟩} has dim ≤ n²+1 — correct; contains constants (α=1, A=0) and coordinates (α=0, A = e_ae_bᵀ, ⟨e_ae_bᵀ, X⟩ = X_ab — recomputed, correct); elements continuous and bounded on the compact X (X is the continuous image of S^{n−1}×S^{n−1} under (a,b) ↦ abᵀ, hence compact and second countable, so (P1)/(P2) and Lemma 1(b) carry over); Lemmas 2–4 use only constants-and-coordinates membership, so entropy ≤ log d ≤ log(n²+1) and E[rX] = cX₀ with c ∈ (0,1] (c = 1/E|k| ≤ 1 from E|k| ≥ 1; c > 0 from E|k| ≤ K^{1/2} < ∞). ACCEPTED.
REMARK C.1: DEFECT (class B) — see FINDINGS rows 2 and 3. The false claims invalidate the quantitative sentence of this remark only. The remark's actual purpose — no conflict with the source's tightness/No-for-nonnegative claims — survives on the weaker true ground that the source's negative result quantifies over worst-case μ, while the construction here is not claimed (and not required) to be signed for every μ. Nothing downstream depends on this remark.
REMARK C.2: the flatness dictionary quote matches source card Q4 verbatim; the observation that r is neither flat nor nonnegative, so no submatrix is named and Theorem 2.4 is untouched, is correct commentary. ACCEPTED.
REMARK C.3: correct commentary — ev_{v₀} and Lemma 1(b) genuinely rely on supp μ, which has no pseudo-distribution analogue; scope delimitation accurate. ACCEPTED.
DEPENDENCIES: all four "external mathematics" items are standard and restated with proof or are one-line facts (op ≤ F; full-measure support; the max computation re-verified above). Source quotes checked hypothesis-by-hypothesis against card S1: Q1 (Question 8.1) is verbatim identical to the Contract's STATEMENT (same normalisation E|r| = 1, same entropy functional, same relative Frobenius criterion, same "not necessarily positive"); Q2, Q4, Q5, Q6, Q7, Q8 quoted in the artifact match the card verbatim. No citation defect. The artifact correctly notes the source is load-bearing only for the "answers Question 8.1" claim.
NUMERICAL CHECK, recomputed from scratch: q_Q values at the four points are (a, c, (a+2b+c)/2, (a−2b+c)/2), so F_μ = {x : x₁+x₂ = x₃+x₄} (normal w = (1,1,−1,−1)), d = 3 ✓. Kernel at v₁: need k ∈ F_μ with ¼k·f = f₁ for all f ⊥ w, i.e. ¼k − e₁ = λw with k·w = 0: 4 + 16λ = 0, λ = −1/4, k = 4e₁ − w = (3,−1,1,1) ✓. E_μ[k] = ¼(3−1+1+1) = 1 ✓. K = ‖k‖²_{L²(μ)} = ¼(9+1+1+1) = 3 = d ✓. E[k vvᵀ] = ¼(3[[1,0],[0,0]] − [[0,0],[0,1]] + ½[[1,1],[1,1]] + ½[[1,−1],[−1,1]]) = ¼[[4,0],[0,0]] = v₁v₁ᵀ ✓ exactly rank one. E|k| = ¼(3+1+1+1) = 3/2 ✓, c = 2/3 ✓, r = (2,−2/3,2/3,2/3) ✓, E|r| = ¼(2+2/3+2/3+2/3) = 1 ✓. Entropy = ¼[2 ln 2 + 2 ln(2/3)] = ½ ln(4/3) = 0.143841 — matches 0.1438 ✓. E[r²] = ¼(4 + 3·4/9) = 4/3, ln(4/3) = 0.287682 — matches 0.2877 ✓; also = ln(c²K) = ln(4/3) ✓. ln 3 = 1.098612 — matches 1.0986 ✓. Chain 0.1438 ≤ 0.2877 ≤ 1.0986 ✓. r genuinely signed in this example ✓. (Note: this same example exhibits E|k| = 3/2 < √3 = K^{1/2}, refuting Remark C.1's equation.) ALL NUMBERS CORRECT.

### SOURCE REQUEST ###
none
