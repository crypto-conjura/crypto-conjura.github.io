MODEL: sonnet

### VERDICT ###
STATUS: DEFECTS
The Theorem/Corollary construction survives an extensive refutation attempt (degenerate, symmetric-discrete, and continuous test measures, plus a full line-by-line algebraic audit) with no counterexample found, but the closing scope Remark C(i) commits a citation-scope mismatch (class D): it discharges "no conflict with Theorem 2.3's tightness" using a derivation valid only for the Main Theorem's narrower PSD/symmetric (vv^T) domain, while Theorem 2.3 and its tightness claim (Q2/Q5) are stated over the *general* rank-one-matrix domain that only Corollary B (not the Main Theorem) actually occupies, and no analogous derivation is given for Corollary B.

### FINDINGS ###

| quoted location | class A-E | explanation |
|---|---|---|
| "Corollary B" + Remark C "(i) No conflict with the source's negative results" | D | Q2 (Theorem 2.3) and Q5 (its tightness) are stated by the card for "any distribution µ over rank one n×n matrices" — an unrestricted (not necessarily symmetric/PSD) domain, which is exactly the domain Corollary B was built to match ("the normalisation used in the source paper's intended application", quoting Q8/§2.3). The Main Theorem, by contrast, only ever outputs `L = c·v0v0^T`, always symmetric PSD. Remark C(i)'s "Derivation of the equivalently clause" explicitly starts "Let `v_0` and `k = k_{v_0}` be as in the proof of **the Theorem**" (the sphere/PSD construction), not Corollary B. So the object used to rebut a conflict with Q5 is not the object living in the domain Q5 is actually about. Concretely, I tried to check whether the derivation "generalizes verbatim" (as Corollary B's own proof claims for the main construction) and found it does **not**: the derivation's key step — forcing `v ∈ {±v0}` from `⟨v,w⟩ = 0 ∀ w ⊥ v0` together with `‖v‖=1` — uses the sphere-specific fact that `vv^T` is 2-to-1 (v and −v give the same matrix) and that quadratic forms are even (`k(−v0)=k(v0)`). Neither fact has an analogue for Corollary B's affine functional space `G` on the manifold `X` of unit-Frobenius rank-one matrices (elements of `G` are linear, not quadratic, in `X`, and `X = ab^T` is *not* 2-to-1 onto itself the way `v ↦ vv^T` is). A structurally different argument (via strict convexity of the Frobenius norm: if `E_{μ'}[X]=X_0` with `‖X_0‖_F=‖X‖_F=1` μ'-a.e., strict convexity forces `X=X_0` μ'-a.e., no ± needed) plausibly closes the gap, but this is *not* the argument given, is not verified in the artifact, and was not attempted there. As written, Remark C(i)'s "no conflict" claim about Q5 is proved only for a strict sub-case of the domain Q5 concerns, so the scope claim overreaches what is shown. |
| "the construction returns a nonnegative `r`... so such a `μ` is not a hard instance for the nonnegative question" | C | Consequence of the D-finding above: this sentence is stated without qualification (reads as covering the artifact's whole reconciliation with the source's negative results), but is established only for the Main Theorem's PSD construction. A reader could reasonably take it to cover Corollary B's setting too, since Corollary B is the one actually matched in domain to Theorem 2.3/Q5; that extension is not shown. |
| "`k(−v_0) = k(v_0) = K(v_0,v_0)` by Lemma 1(c)" (inside the Remark C(i) derivation) | — (minor, not scored) | Mislabeled internal citation: `k(−v_0)=k(v_0)` is not what Lemma 1(c) gives (Lemma 1(c) is the Riesz-representer/reproducing property); it is simply evenness of a quadratic form, `q_Q(−v)=q_Q(v)`. `K(v_0,v_0)=k(v_0)` is Lemma 1(c)/definition. The equality chain is correct, only the attribution of one link is imprecise. Does not affect correctness. |
| Theorem, Lemmas 1–4, Corollary (main line) | none found | See STEP LOG: refutation attempt failed. |

### STEP LOG ###

**Refutation attempt on the main Theorem (§ per task instructions).**

1. n=1 sanity check: `S^0={-1,1}`, any μ. `F_μ` is 1-dimensional (only constants, since `v²=1`), kernel `k≡1`, giving `r=1`, exact `L=v0v0^T=1`, entropy `0 ≤ log(n(n+1)/2)=0`. Consistent, trivial, accepted.

2. Degenerate atomic measure: `n=2`, `μ=δ_{e1}`. `F_μ` collapses to 1‑dim (only the value at `e1` matters), `k=[1]`, `K(v0,v0)=1`, `r=1`, exact `L=e1e1^T`, entropy `0`. Consistent, no refutation.

3. Two-point discrete measure (asymmetric evaluation): `n=2`, `μ=(δ_{e1}+δ_{e2})/2`. Computed `F_μ` (2‑dim, the off‑diagonal quadratic form vanishes on both points), kernel at `v0=e1` is `k=(2,0)` (values at `e1,e2`). Verified `E_μ[k]=1`, `E_μ[k\,vv^T]=e1e1^T` exactly, `K(v0,v0)=2=d`, entropy `=log2 ≤ log(n(n+1)/2)=log3`. Consistent, no refutation. (Here `r` happens to be nonnegative — the "easy instance" case addressed in Remark C(i).)

4. Symmetric 3-point discrete measure: `n=2`, uniform on three points at `0°,120°,240°`. Verified `F_μ` is the full 3-dim space of quadratic forms (orthogonal basis `1,cos2θ,sin2θ`), reproducing kernel `k(θ)=1+2cos(2(θ-θ0))` takes the values `(3,0,0)` at the three points for `v0` at `θ=0`, giving `K(v0,v0)=3=d` exactly (Lemma 3 tight here), `E_μ[k]=1`, `E_μ[k\,vv^T]=v0v0^T` exactly (trivially since `k` vanishes at the other two points), entropy `=log3` exactly equal to the stated bound `log(n(n+1)/2)=log3` (equality case of Lemma 4, matching the "indicator-type" sharpness note in the artifact). Consistent, no refutation; this is also a genuine equality/tightness check on Lemma 3 and Lemma 4 simultaneously, and it held exactly.

5. **Continuous measure**: `n=2`, `μ=` uniform (Lebesgue) measure on the full circle `S^1`. Derived analytically `F_μ=\mathrm{span}\{1,\cos2θ,\sin2θ\}`, `d=3=n(n+1)/2` (full-dimensional), reproducing kernel `k_{v0}(θ)=1+2\cos(2(θ-θ0))`, which is **genuinely signed** (ranges over `[-1,3]`, negative on an arc of length `2π/3` centered at the antipode of `2θ0`). Verified by direct integration that `E_μ[k]=1` and, entry by entry, `E_μ[k\,v_1^2]=\cos^2θ_0`, `E_μ[k\,v_2^2]=\sin^2θ_0`, `E_μ[k\,v_1v_2]=\cos θ_0\sinθ_0`, i.e. `E_μ[k\,vv^T]=v_0v_0^T` **exactly**, confirming Lemma 2(i) on a genuinely continuous, non-degenerate measure requiring real cancellation. Computed `E_μ|k| = \frac{1}{2π}\int_0^{2π}|1+2\cosφ|\,dφ ≈ 1.4362` (closed form `\frac{2π/3 - ... }{}`, numerically verified via the zero-crossings of `1+2\cosφ` at `φ=2π/3,4π/3`), giving `c≈0.6963`, entropy `≈0.374 ≤ \log 3 ≈1.0986`. Consistent with the Theorem; no refutation, and this is the "requires cancellation" regime the Contract's Known Boundary flags as the hard case, yet the bound held with room to spare.

6. Re-derived Lemma 1(a)–(d), Lemma 2(i)–(iii), Lemma 3, Lemma 4 symbolically line by line (support-consistency argument via continuity + second countability; Riesz representer construction; the trace identity `∫K(v,v)dμ=d`; the `t\log t ≤ t^2` / Jensen argument for the entropy bound). Found each step to be a correct, if elementary, application of finite-dimensional reproducing-kernel theory (the "Christoffel function" identity `∫K(v,v)dμ = \dim` is a standard fact, correctly proved from scratch here). No gap found.

7. Cross-checked the artifact's own "NUMERICAL CHECK" (n=2, four points) arithmetically end to end (kernel values, `E_μ[k]`, `K(v_0,v_0)`, `E_μ[k\,vv^T]`, `c`, `r`, entropy, `\log E[r^2]`, `\log 3`) — every number reproduced exactly as stated.

8. Verified the Corollary's calculus (`n(n+1)/2 \le n^2` for `n\ge2`; `\max_{x>0}(\log x)/x^\delta = 1/(e\delta)` at `x=e^{1/\delta}`) — correct.

9. Verified Remark A's algebra (quadratic-in-`t` feasibility condition, positivity of both roots) — correct, and explicitly marked non-load-bearing by the artifact itself.

**Conclusion of refutation attempt: failed.** I could not construct an explicit `(n,\mu)` for which the stated construction fails to deliver exact rank-one recovery within the claimed entropy bound `\log(n(n+1)/2)`, across degenerate (point-mass), symmetric-discrete (2- and 3-point, both giving equality cases), and continuous (uniform-on-circle, genuinely requiring signed cancellation) test measures. I flag for the record that the Theorem, if correct as written, proves substantially more than Question 8.1 as posed (exact, zero-error recovery at `O(\log n)` entropy, for every `\delta>0`, not merely `\delta<1/2`), which is a strong claim relative to a question the cited source explicitly calls "very interesting" if resolved for any `\delta<1/2` — this is worth an editor's independent re-check, but I report the failed refutation as a failed refutation, not as a defect, per instructions.

**Source fidelity (class D) pass, hypothesis by hypothesis.**

- Q1 (Question 8.1) and its follow-on sentence: quoted verbatim, character-for-character identical to the card, complete (no dropped clause). Used correctly: the Contract's conjecture is confirmed to match Q1 "with no additional hypothesis," which is accurate.
- Q2 (Theorem 2.3): quoted verbatim, complete. Used to build Corollary B, with the correct domain-matching normalization (unit-norm `a,b`) pulled from Q8/§2.3, also quoted verbatim and used with its actual hypothesis (the parenthetical about the restriction being "easy to lift" is quoted but not leaned on for anything beyond motivating the normalization choice — no overreach).
- Q9 (Definition 2.2, "deficient"): quoted verbatim, complete. The gloss "a `k`-deficient reweighting is nonnegative and normalised" is directly supported by the card's own annotation and by the definition's requirement that `µ'` be "a probability distribution" — correctly within the card's words, no overreach.
- Q4 (flatness dictionary, §2.2): quoted verbatim, complete, used narrowly and correctly in Remark C(ii) only to say the route is *unavailable* (the Theorem gives no flatness), not to draw any positive log-rank consequence — appropriately conservative, no overreach.
- Q5 (tightness of Theorem 2.3): quoted verbatim, complete. **This is the quotation implicated in the D-finding above**: the artifact's reconciliation of "no conflict" with Q5 is proved only for a sub-domain of the object Q5 is about (see FINDINGS table). This is not a misquotation but a misapplication — the hypotheses under which the reconciling argument is run (Main Theorem's PSD `vv^T` output) do not match the hypotheses of the quoted claim (general rank-one matrices, Theorem 2.3's domain).
- Q6/Q7 (footnotes 7 and 8): quoted verbatim, complete; Q7's gloss ("moot here: our constant is independent of `ε`") is a claim about the artifact's *own* result, correctly derived from the Corollary (`C(ε,δ)=2/(eδ)` has no `ε`-dependence) — not an overreaching claim about the source.
- Q8 (§2.3 normalisation): quoted verbatim, complete, correctly used as the definition of `X` in Corollary B.
- No quotation was found to be truncated, paraphrased-as-verbatim, or missing a qualifying clause; no technical term glossed by the artifact was found to go beyond the card's own words, except for the Q5/Theorem 2.3 domain-scope issue identified above.

### SOURCE REQUEST ###
No additional external source is needed; the card fully covers every quotation used. What is missing is *internal* to the artifact: either (a) Remark C(i)'s "no conflict" claim should be explicitly restricted to the Main Theorem's PSD/symmetric sub-domain (dropping any implication that it also reconciles Corollary B, the domain that actually matches Theorem 2.3/Q5), or (b) the artifact should supply the (necessarily different, strict-convexity-based) derivation showing the analogous atom/entropy identity for Corollary B's domain `X`, if it wishes to claim "no conflict" with Q5 in full generality.
