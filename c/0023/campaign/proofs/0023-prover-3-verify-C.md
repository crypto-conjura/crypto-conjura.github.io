id: 0023-prover-3-verify-C
agent: verifier
model: claude-sonnet-5
cycle: 3

# Verification report: 0023-prover-3.md

## 1. FINAL VERDICT

**CLEAN (no critical error, no statement drift, no negation-check contradiction).** The artifact's completeness gate passes (`### END OF ARTIFACT 0023-prover-3 ###` present, no mid-sentence truncation); its two caps (CAP I, T6; CAP II, T8), the two witness tables (T5, T7), the P1/P5 escape analyses (§7), the randomised-window extension (Remark 2.2), the finite-abelian-group generalisation (§5.4), and the two calibration claims re-derivable from this artifact's own machinery (§6.1, §6.2) all check out under independent exact-arithmetic re-derivation by hand (k=1 in witness (a): d=2; d=2 in witness (b); a Z_3, d=1 spot-check of §5.4's group formula). The class-defeating quantifier ("one witness pair against every W meeting the density/own-heavy hypothesis," not "a witness chosen per-W") is genuinely established, not merely asserted. I file three non-blocking notes below (one class C imprecision in the VERDICT summary, one class D over-citation, one class E non-load-bearing missing-card note) and one explicit scope caveat that Triage/a human should track (fidelity of the artifact's P1/P5 definitions to the Strategist's actual named plans, which is outside what a blind verifier is permitted to check).

## 2. FINDINGS TABLE

| # | Location | Class | Explanation |
|---|---|---|---|
| 1 | VERDICT clause 2, "every own-heavy window functional at level θ≥1/poly(d) is capped at 1/(2(2^d-1))" | (C) minor, pedantic | The precise result (T8 + Remark 4.3) requires θ > 1/(2(2^d-1)) and, for a fixed θ=1/poly(d), the cap only bites "for all d≥d₀(θ)" (Remark 4.3), not literally every d≥1. The one-line VERDICT elides this quantifier; the body is careful. Not load-bearing since the artifact makes no claim about a witnessing δ for R2. |
| 2 | §7.1 and DEPENDENCIES table, card S7b (Chang–Fang) cited for "$|W_{\mathrm{sh}}(A)|\le d$" | (D) minor, non-load-bearing | Confirmed by web search that Chang–Fang's actual theorem (arXiv:2510.13705) is the much stronger shattering statement ("supp(f) shatters $S^c$"), which the artifact explicitly disclaims using ("the projection property is not used"). The fact actually invoked — a maximum-degree monomial's support has size exactly $\deg(f)\le d$ — is immediate from the artifact's own D1 definition of degree and needs no citation at all. Over-citing, not mis-citing: no defect in the escape argument, since the weaker fact used is trivially true. |
| 3 | DEPENDENCIES table, cards S6a/S6c/S6d/S7b | (E) non-blocking | Could not locate these card files under `c/0023/campaign/sources/` (no directory-listing tool available to this verifier; several plausible filenames were tried and failed to resolve, in contrast to S1, which resolved and was read). None of these four is load-bearing per the artifact's own table (S6a's fact is "re-proved inline" as T4(c); S6c, S6d marked "no"; S7b's fact is trivial per finding #2). Recorded for the record; does not block acceptance of CAP I/CAP II, which are self-contained. |
| 4 | Scope of this review vs. §0(iv)/§7 (P1, P5 definitions) | scope caveat, not a defect | This pass verified only that P1 ("shattering window") and P5 ("certificate window") as *defined inside this artifact* escape CAP I/CAP II, and did so correctly (see log). It could not check — and was not permitted to check, per the harness's blind-verifier design and this task's file restriction to CONTRACT/I02/sources — whether these definitions faithfully instantiate the Strategist's actual named plans P1/P5 (`0023-strategist-2`, out of scope). Since the artifact's own headline calls this "the campaign-critical finding," a Triage step with access to `0023-strategist-2` should confirm plan fidelity before treating "the campaign's lead plan is not dead" as established beyond this artifact's internal logic. |

No class (A) statement drift and no class (B) critical error were found anywhere in T1–T9, §5, §6, §7, or §8.

## 3. STEP-BY-STEP LOG

**STEP 0 (completeness).** `### END OF ARTIFACT 0023-prover-3 ###` present; no unit stops mid-sentence/equation. PASS.

**Class (A) check — what is actually proved vs. the Contract/I02.** The artifact proves neither R2 nor its negation and says so explicitly ("No claim is made about R2's truth... R2 remains open"). It is a BARRIER result (HARNESS §3.2 class (f)) against a *stipulated* technique class 𝒲 (D4), explicitly and repeatedly flagged as non-exhaustive ([G1], with concrete named escapees in §6.3 and §7). The title's informal phrase "per-coordinate window-payment arguments" matches D4's formalisation (the T1–T3 skeleton, which is exactly the shape of R1's engine and of K1's proof mechanism, card S1.b). No drift between headline and body found.

**STEP 0.5 (negation check).** Tested candidate tension points for a hard-stop contradiction:
- Is $W_{\mathrm{Forced}}$ both "capped" (via witness (b), §6.2) and "not capped" ($V=0$, Remark 4.3) simultaneously asserted without qualification? No: witness (b) gives a *nonzero* ratio $1/(2(2^d-1))$ *on that pair only*; witness (a) gives *both windows empty* on $(A_k,B_k)$, which by D5's own convention forces $V_{W_{\mathrm{Forced}}}(d):=0$ over the full $\mathcal P_d$ (the inf includes this pair). No contradiction — I independently re-derived both facts from T5's and Remark 4.2's own formulas (see below) and confirmed they are compatible, not contradictory: $V_W(d)=0$ is consistent with *some other pair* scoring higher, since $V_W$ is a minimum.
- Is $W_{\max}$ (§7.3) both "own-heavy" (by D2's existential reading, trivially at its own realized level) and "not own-heavy at level ≥1/(2(2^d-1))" (needed to escape CAP II)? Checked: $W_{\max}$'s only *guaranteed* own-heavy level is the T4(c) floor $2^{-d-1}$, and $2^{-d-1} < 1/(2(2^d-1))$ for all $d\ge1$ (checked algebraically), so CAP II's hypothesis genuinely fails for $W_{\max}$ — no contradiction with the escape claim.
- $\eta^*(d)\in[2^{-d},1]$ (T4(d)) vs. both certified witnesses giving $\Theta(d)$ values (§8.4): consistent, since $\eta^*$ is an infimum and the grid pair (a *third*, different construction, not witness (a) or (b)) is what attains $\eta^*\le1$; the open question ("is there a pair with $\pi_{\mathrm{Rel}}=2^{-\Theta(d)}$?") is honestly left open in §8.3, not asserted either way.
No lemma and its negation were both accepted. PASS.

**T1 (master count).** Proof re-checked: uses only independence (each window depends on its own function) and linearity; finite sums (finitely-supported hypothesis). Correct. Tag: **MECH**.

**T2 (value theorem).** Re-checked: evaluating at point-mass families is a legitimate member of the family space and gives the sharpest possible upper bound on what (T1)–(T3) can deliver; the empty-window edge case is handled correctly via D5's convention. Tag: **MECH**.

**T3 (localisation ceiling).** Re-checked: $W(B)\subseteq \mathrm{Rel}(B)$ and $\mathrm{Inf}_i(f_A)=0$ off $\mathrm{Rel}(A)$ give the displayed inequality termwise; correct. Tag: **MECH**.

**T4 (floor).** (a) Combinatorial-Nullstellensatz-type restriction argument re-derived by hand from scratch (traced the coefficient of the top monomial through a coordinate restriction) — correct. (b) Re-derived: if $\mathrm{Rel}(A)\cap\mathrm{Rel}(B)=\emptyset$ one can independently satisfy $A$ on $\mathrm{Rel}(A)$ and $B$ on $\mathrm{Rel}(B)$, contradicting the $A\cap B=\emptyset$ clause built into $\mathcal P_d$'s own definition — correct, and this is exactly why $\mathcal P_d$'s disjointness hypothesis is essential here (checked the counterexample structure this guards against: two juntas on disjoint coordinate sets, which the disjointness hypothesis rules out of $\mathcal P_d$). (c) Hand-verified the $2^{-d-1}$ influence-quantum bound via the discrete-derivative argument and Parseval; matches the (independently, non-load-bearingly) cited NS94/S6a fact. (d) Lower/upper bounds re-derived; upper bound $\eta^*\le1$ correctly attributed to a third ("grid") construction, distinct from witnesses (a)/(b). Tag: **MECH** throughout (S6a citation present but explicitly non-load-bearing, "reproved inline").

**T5 (witness (a), address pair) — independently re-derived by hand at $k=1$ ($d=2$, $N=3$).** Built the truth table of $A_1=\{x: y_{b(a)}=+1\}$ directly, verified the multilinear expansion matches T5(i) at all 8 points, confirmed $\deg=2$, $\alpha=1/2$, $\mathrm{Rel}=\{a_1,y_0,y_1\}$, $\mathrm{Inf}_{a_1}(f)=1/4$, $\mathrm{Inf}_{y_j}(f)=1/4$, $\pi_{\mathrm{Rel}}(A_1,B_1)=3/2$ — all match T5 and the artifact's own reported $k=1,2,3$ table exactly. Tag: **MECH + CODE** (also cross-checked against the logic of `0023-prover-3-code/check_witnesses.py`, read in full; its exact-`Fraction` computation of influences, degrees, $\pi_{\mathrm{Rel}}$, and shattering/certificate payments matches the hand derivation structurally).

**T6 (CAP I).** Re-derived: $\Theta_W(d)\le$ ratio at $(A_k,B_k)$ by definition of an infimum; substituting T5(v)'s exact value and the density/⊇-Rel hypotheses on *this specific pair* (not a per-$W$-chosen pair) gives the stated bounds (a)–(c). Confirmed algebraically that $(d+1)/(4(2^{d-1}+d-1))\le(d+1)2^{-d-1}$. **This is the "single witness defeats every $W$ satisfying the hypothesis" form, correctly established** (the density/⊇-Rel hypothesis is universally quantified over "every $A$" in D2, hence applies in particular at $A_k,B_k$, giving the needed size control for *any* $W$ in the sub-class at once). Tag: **MECH**, built on T5.

**T7 (witness (b), subcube pair) — independently re-derived by hand at $d=2$.** Built $\mathbf 1_C=(1+x_1)(1+x_2)/4$, computed $\mathrm{Inf}_1(f_C)=1/2$, $\mathrm{Inf}_1(f_D)=1/6$, $\pi_{\mathrm{Rel}}(C,D)=4/3$ — matches T7 and the artifact's reported $d=2$ table exactly. Tag: **MECH + CODE**.

**T8 (CAP II).** Re-derived: own-heavy hypothesis (universally quantified "for every $A$" in D2) forces $W(D)=\emptyset$ at the *specific* pair $(C,D)$ since every $\mathrm{Inf}_i(f_D)=1/(2(2^d-1))<\theta$; correct, and again the "one witness for all qualifying $W$" form. Tag: **MECH**, built on T7.

**T9 (dichotomy).** Algebra re-derived exactly: $w_D=0<w_C\Rightarrow$ ratio $=1/(2(2^d-1))$; $w_D\ge1\Rightarrow \pi_W\ge1/2\Rightarrow$ ratio $\ge1/(2(w_C+w_D))\ge1/(4d)$. Correct; the "iff" is scoped to "as witness (b) sees it" (title makes this explicit), not over-claimed as a statement about $V_W(d)$ globally. Tag: **MECH**.

**§5.1–5.3 (scope / monotonicity).** Re-checked the *direction* of the containment argument, which is the easy place to invert: $\mathcal C\subseteq\mathcal C'\Rightarrow\mathcal P_d(\mathcal C)\subseteq\mathcal P_d(\mathcal C')\Rightarrow V^{\mathcal C'}_{W'}(d)\le V^{\mathcal C}_W(d)$ (infimum over a superset is smaller-or-equal). The artifact states this correctly and uses it in the correct direction twice: (i) to explain why CAP I/II do **not** condemn R1 (a witness inside the *larger* class $\mathcal C^{\mathrm{ind}}_d$ does not transfer *down* to the smaller class $\mathcal C^{\mathrm{junta}}_d$ that does not contain it — checked $|\mathrm{Rel}(A_k)|=2^{d-1}+d-1>d$, so $A_k\notin\mathcal C^{\mathrm{junta}}_d$, correct); (ii) to transfer CAP I/II *upward* from R2's witnesses to R3–R6′ (witnesses lie in $\mathcal C^{\mathrm{ind}}_d\subset\mathcal C^+_d\subset\dots$, so they remain valid pairs in the larger $\mathcal P_d$ of every larger class). Both directions checked and correct; this is exactly the kind of monotonicity direction that is easy to get backwards, and it is not backwards here.

**§5.4 (group generalisation) — independently spot-checked at $\mathcal Y=\mathbb Z_3$, $d=1$.** Computed $\alpha_C=1/3$, $\mathrm{Inf}_1(f_C)=2/3$ (matches claimed $1-|\mathcal Y|^{-1}$), $\mathrm{Inf}_1(f_D)=1/3$ (matches claimed $(1-|\mathcal Y|^{-1})/(|\mathcal Y|^d-1)=(2/3)/2$). CAP I's even-order pullback is correctly justified (a finite abelian group has a $\mathbb Z_2$ quotient iff its order is even, by Cauchy's theorem for abelian groups); the odd-order gap is honestly flagged as [GAP-2] and used in no claim. Tag: **MECH**.

**§6 (calibration).** §6.1 (I01/R1 not condemned): re-derived using only the frozen R1 statement as recorded in I02's "Frozen inputs" section (permitted context) — $\mathcal C^{\mathrm{junta}}_d\not\ni A_k$ since $|\mathrm{Rel}(A_k)|>d$, so CAP I's witness does not bind R1's class; correct by §5.2's monotonicity direction. §6.2 (refuter-3's $1/(8d)$ bound not condemned): I could not check the characterisation of refuter-3's actual argument (out of scope), but I *could* and did independently verify the two internal sub-claims it rests on using only this artifact's own T5/T7 machinery: (i) $\mathrm{Forced}(A_k)=\mathrm{Forced}(B_k)=\emptyset$ (no coordinate of the address pair is ever forced to a single value, checked directly against the T5(i) formula), giving $V_{W_{\mathrm{Forced}}}(d)=0$ over the full $\mathcal P_d$ by D5's convention; (ii) on witness (b), $W_{\mathrm{Forced}}(C)=[d]$, $W_{\mathrm{Forced}}(D)=\emptyset$, giving ratio $1/(2(2^d-1))$, consistent with (not contradicting) refuter-3's bound being scoped to "forcing pairs" only. Both check out; **CAP II does not refute the forcing-bound result**, because that result was never claimed on the general $\mathcal P_d$ in the first place — a genuinely different (narrower) quantifier scope, correctly identified. This is the single highest-value check requested by the task, and it comes out clean. §6.3 (P4/K1/P2 outside the class) and §6.4 (consistency vs. K1, card S1) re-derived algebraically: $2^{-d}/d\le(d+1)2^{-d-1}$ for $d\ge1$ and $2^{-d}/d\le1/(2(2^d-1))$ for $d\ge2$, both confirmed by direct computation.

**§7 (CLAUSE (iv), P1/P5 escape) — the campaign-critical finding.** Re-derived the exact payment $k/2+2^{-k}$ for the shattering window on witness (a) and confirmed the ratio bound $\ge1/8$ algebraically (via $k/(k+1)\ge1/2$ for $k\ge1$). Re-derived §7.2's non-canonicity theorem: the automorphism group $\{\sigma_c\}$ acts transitively on the $2^k$ targets while fixing $A_k,B_k$ setwise, so any *equivariant* localised $W$ must select all-or-none of the targets — correct, and this shows exactly what the price of the escape is (loss of canonicity), consistent with D1's canonicity being optional for class membership (only localisation is required, per D4). Re-derived §7.4/§7.5's certificate-window computations, including the worst-case-vs-average distinction: worst-case payment $2^{-k}$ (attained when addresses agree) vs. average $k/4+4^{-k}$ (both independently re-computed from the uniform-address-vector model) — correct, and the conclusion "P5 must stay averaged" is a properly-justified, honestly self-flagged drafting constraint (not swept under the rug). **Scope caveat**: verified only that these definitions, as stated in this artifact, escape CAP I/CAP II; could not check fidelity to `0023-strategist-2`'s original P1/P5 (out of scope, see Findings #4).

**§8 (frontier).** The universal ceiling ($V_W\le\eta^*$, all localised $W$) and the one-directional "sufficiency" claim (§8.3) are correctly not converse-claimed; [G3] flags this. The milestone ("search for a pair with $\pi_{\mathrm{Rel}}<1$") is a well-posed, falsifiable, strictly-easier-than-(PAY⋆) computational target, correctly derived from T3+T4(d).

**GAP REGISTER.** [G1]–[G4] read as honest and complete: [G1] correctly frames D4 as a stipulation, not exhaustive, with named escapees listed; [G2] (odd order) matches what my own check of §5.4 found; [G3], [G4] are routine and accurately described. No further unflagged gap was found beyond Findings #1–#3 above (all non-blocking).

## 4. SOURCE REQUEST

None required to adjudicate the mathematics (CAP I, CAP II, and the escape arguments are self-contained and were independently re-derived). For the record only (non-blocking, per Finding #3): the card files for S6a, S6c, S6d, and S7b could not be located by this verifier under `c/0023/campaign/sources/` (no directory-listing tool was available; several plausible filenames failed to resolve, unlike S1 which resolved and was read). If a future pass has directory access, confirming these four cards exist and match their cited content (especially S7b, whose cited fact I independently confirmed is a trivial consequence of the artifact's own D1, not something requiring S7b's actual theorem) would close this loose end, but nothing in this artifact's proofs depends on it.

### END OF ARTIFACT 0023-prover-3-verify-C ###
