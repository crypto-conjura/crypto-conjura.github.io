---
id: "0003"
problem: k-collisions-auxiliary-input
draft: k-collisions-aux
title: "Tight Time-Space Tradeoffs for $k$-Collisions with Auxiliary Input"
short_title: "Tight $k$-collision time-space tradeoff"
areas: [symmetric-key]
topics: [collision-finding, time-space-tradeoffs, random-oracle-model]
model: rom
form: tight-bound
assumption_class: unconditional
category: research-open
revision: 1
status:
  statement_informal: ai
  statement_formal: open
  statement_match: open
  proof_informal: open
  proof_review: ai
  proof_formal: open
# status_badge is generated by scripts/status_badge.py -- do not hand-edit
difficulty:
  reach: new-idea
  by: ai
  note: "Upper and lower bounds meet only at k=2, and for k>=3 neither side has been made tight, so no existing technique currently reaches the answer from either direction."

status_summary: "The exact tight time-space tradeoff for k>=3 collisions under preprocessing is open; only the k=2 case has a matching upper and lower bound."
status_updated: 2026-08-19
relations: []
lean:
  mode: in-repo
  path: lean/Statement.lean
  decl: k_collision_time_space_tradeoff
sources: []
aliases:
  - "/open-problems/symmetric-key/k-collisions-auxiliary-input/index.html"
statement_sha: "71648ddc2a88a44ef54757a1e57d16e9125633172e0b467c6ede989b01fd7e36"
status_badge: "<a href='https://crypto-conjura.github.io/open-problems/status-legend/index.html#status-legend' class='cj-status-link' aria-label='Statement: AI-written, not yet formalized. Proof: open -- no attempt yet. Click for the status badge legend.'><svg class='cj-status-badge' viewBox='0 0 32 32' width='28' height='28' xmlns='http://www.w3.org/2000/svg' role='img'><circle cx='16' cy='16' r='13' fill='none' stroke-width='4' class='cj-ring-0' stroke-dasharray='1,3'/><circle cx='16' cy='16' r='8' class='cj-disc-0'/><text x='16' y='17' text-anchor='middle' dominant-baseline='central' class='cj-glyph' font-size='9'>–</text></svg></a>"
---

<!-- status:start -->
<a href='https://crypto-conjura.github.io/open-problems/status-legend/index.html#status-legend' class='cj-status-link' aria-label='Statement: AI-written, not yet formalized. Proof: open -- no attempt yet. Click for the status badge legend.'><svg class='cj-status-badge' viewBox='0 0 32 32' width='28' height='28' xmlns='http://www.w3.org/2000/svg' role='img'><circle cx='16' cy='16' r='13' fill='none' stroke-width='4' class='cj-ring-0' stroke-dasharray='1,3'/><circle cx='16' cy='16' r='8' class='cj-disc-0'/><text x='16' y='17' text-anchor='middle' dominant-baseline='central' class='cj-glyph' font-size='9'>–</text></svg></a>

*Statement: AI-written, not yet formalized. Proof: open -- no attempt yet.*
<!-- status:end -->

<!-- topics:start -->
<div class="cj-tags cj-page-topics"><a class="cj-tag cj-tag-topic" href="/problems/by-topic/collision-finding/">Collision Finding</a><a class="cj-tag cj-tag-topic" href="/problems/by-topic/random-oracle-model/">Random Oracle Model</a><a class="cj-tag cj-tag-topic" href="/problems/by-topic/time-space-tradeoffs/">Time Space Tradeoffs</a></div>
<!-- topics:end -->

::: {.panel-tabset}

## Statement

[View PDF](pdf/main.pdf) · [LaTeX source](latex/main.tex) · [Formal statement](lean/Statement.lean)

### Abstract

Finding collisions in hash functions under preprocessing (the Random Oracle Model with Auxiliary Input, AI-ROM) models realistic adversaries who execute unbounded offline computation to store an $S$-bit summary of a random oracle $H : [N] \to [N]$. While the exact tight tradeoff for 2-collisions ($k=2$) is established as $S \cdot T^2 = \Theta(N)$, determining the exact asymptotic curve $S^a T^b = \Omega(N^c)$ for multicollisions ($k \ge 3$) remains an open problem in provable security.

### Formal Model: AI-ROM

Let $H : [N] \to [N]$ be a uniformly random function, where $N = 2^n$.

::: {.callout-note title="Definition ($k$-Collision Finding with Precomputation)"}
A two-stage adversary $\mathcal{A} = (\mathcal{A}_1, \mathcal{A}_2)$ operates as follows:

i.  *Offline Phase:* $\mathcal{A}_1^H(1^n)$ has unbounded access to $H$ and outputs an advice string $S \in \{0,1\}^S$ of size at most $S$ bits.
ii. *Online Phase:* $\mathcal{A}_2^H(S)$ receives $S$, makes at most $T$ query evaluations to $H$, and outputs a tuple of $k$ distinct inputs $(x_1, x_2, \dots, x_k)$.

The adversary wins if $H(x_1) = H(x_2) = \dots = H(x_k)$ and $x_i \neq x_j$ for all $1 \le i < j \le k$.
:::

### The Open $k$-Collision Tradeoff Conjecture

::: {.callout-important title="Conjecture (Tight $k$-Collision Time-Space Tradeoff)"}
Let $H : [N] \to [N]$ be a random oracle. For any $k \ge 3$, any adversary $(\mathcal{A}_1, \mathcal{A}_2)$ with $S$-bit advice and $T$ online queries that finds a $k$-collision with success probability $\epsilon \ge 1/2$ must satisfy:

$$
S^{k-1} \cdot T^{k} \ge \Omega\left( N^{k-1} \right).
$$

Specifically, for 3-collisions ($k=3$), the optimal trade-off satisfies:

$$
S^2 \cdot T^3 \ge \Omega\left( N^2 \right).
$$
:::

### Comparison of Known Upper and Lower Bounds

| Collision Multiplicity ($k$) | Known Lower Bound | Best Known Attack (Upper Bound) | Status |
|---|---|---|---|
| $k = 2$ (2-Collision) | $S \cdot T^2 = \Omega(N)$ | $S \cdot T^2 = O(N)$ ([Hellman, 1980](https://www-ee.stanford.edu/~hellman/publications/36.pdf) / [Coretti et al., 2018](https://eprint.iacr.org/2017/937.pdf)) | Tight |
| $k = 3$ (3-Collision) | $S \cdot T^3 = \Omega(N)$ | $S^2 \cdot T^3 = O(N^2)$ | *Gap of $S$* |
| General $k \ge 3$ | $S \cdot T^k = \Omega(N)$ | $S^{k-1} T^k = O(N^{k-1})$ | *OPEN* |

: Current state of knowledge for precomputation bounds in AI-ROM.

*Why the problem is hard:* current lower-bound techniques (such as compression arguments or the bit-fixing model) fail to capture the multi-way combinatorial synergy when $S$ advice bits store structural information about hypergraphs of preimages across $k$ distinct points simultaneously.

### Sources

- [Hellman. *A Cryptanalytic Time-Memory Trade-Off* (IEEE Trans. Inf. Theory 26(4), 1980)](https://www-ee.stanford.edu/~hellman/publications/36.pdf). The origin of the tradeoff question.
- [Coretti, Dodis, Guo and Steinberger. *Random Oracles and Non-Uniformity* (EUROCRYPT 2018)](https://eprint.iacr.org/2017/937.pdf). The presampling technique the known bounds go through.

## Proof

View PDF — *no proof written yet* · LaTeX source — *no proof written yet* · Formal proof — *not yet formalized*

This conjecture is open — no proof exists yet.

Once (an attempt at) a proof exists, add its own `.tex` source and compiled PDF (e.g. `latex/proof.tex` / `pdf/proof.pdf`) and link them here, following the same convention as the statement.

**Formal artifact.**

[Lean statement](lean/Statement.lean) — autoformalized by Claude Sonnet 5; not yet checked by a human against the tex statement above.

Only the conjecture's *statement* has been formalized so far (as a Lean theorem ending in `sorry` — see `lean/Statement.lean`), not a proof. Once a proof exists, set `status.proof_informal`/`status.proof_review`/`status.proof_formal` accordingly and regenerate the badge with `scripts/status_badge.py`.

## Discussion

The $k = 2$ case is settled at $S \cdot T^2 = \Theta(N)$, and the conjecture is that the curve for $k \ge 3$ is the natural extrapolation of it. What makes that non-obvious is that the offline stage is unbounded: the advice string may encode any function of the whole oracle, so a tight bound has to hold against preprocessing that is not query-bounded at all, which is exactly what presampling techniques were built to handle and what limits how far they reach.

Nothing beyond this has been written here; see the [problem page](/p/k-collisions-auxiliary-input/) for the surrounding results.

**Checked against the literature, 2026-08-19.** Searched for 2022–2024 progress on tight time-space tradeoffs for $k \ge 3$ collisions under unbounded preprocessing. Found substantial progress on a closely related but distinct problem, the "STB conjecture" for $B$-block-bounded multicollisions in Merkle–Damgård hashing (Ghoshal and Komargodski, CRYPTO 2022; Akshima, Guo and Liu, CRYPTO 2022), but that bounds collisions of bounded length in an *iterated* MD construction, not generic $k$-way collisions on a plain random oracle with unbounded preprocessing, which is what this statement asks. No paper found giving a tight bound for the generic (non-MD) $k \ge 3$ case; the conjecture remains open.

## Open obligations

- [ ] Formalize the two-stage adversary game (AI-ROM) in Lean; only the bare conjecture statement has been formalized so far.
- [ ] Close the gap between the $S \cdot T^k = \Omega(N)$ lower bound and the $S^{k-1}T^k = O(N^{k-1})$ best known attack, for general $k \ge 3$.

:::
