MODEL: opus

### VERDICT ###
STATUS: CLEAN
The artifact carries its `### END OF ARTIFACT [T-1] ###` line, proves a statement strictly stronger than the Contract with the quantifiers in the Contract's order, and every step (Lemmas 1–4, Theorem, Corollary, Remark A, Corollary B, Remark C, numerical check) checks out; the only things I can report are class-(C) editorial/routine items that a competent reader fills without effort, and I state explicitly that none of them forces DEFECTS.

### FINDINGS ###

| quoted location | class A-E | explanation |
|---|---|---|
| Corollary B, "Only two properties of the function space were used: it contains the constant `1` ... and it contains each coordinate function" | C (routine) | Literally false as an exhaustive list. Lemma 1(a)/(b) also need each element of `G` to be bounded on the domain and continuous (the *next* sentence does supply exactly these, so the overstatement is self-repaired); Lemma 1(c) needs `dim G < ∞`; Lemma 3 needs the domain second countable (for `μ(supp μ) = 1` via (P1)) and needs `g = Σ_j q_j²` Borel — here `g` is a polynomial in the entries of `X`, immediate but unstated. Compactness of `X` is asserted ("`X` (compact)") without the one-line argument that a limit of rank-one matrices of Frobenius norm 1 has rank exactly 1. All routine; the substance of Corollary B is unaffected. |
| Remark C(i), "— equivalently, `k ≥ 0` forces `μ({±v_0}) = 1/K(v_0,v_0) ≥ 2/(n(n+1))` —" | C (routine) | "Equivalently" mislabels an implication. What the appended derivation establishes is `k ≥ 0` ⟹ `μ({±v_0}) = 1/K(v_0,v_0)`, which is *stronger* than (and implies, via conditioning) the preceding clause; the two clauses are not equivalent. No claim in the Remark becomes false; only the connective is wrong. |
| DEPENDENCIES, "**Source, READ (local copy `the published PDF`, text layer extracted with `pdftotext -layout`).**" | C (routine, editorial) | The sole external source is never identified bibliographically inside the artifact: no authors, title, venue, arXiv identifier or version. Every quotation is nevertheless verbatim-identical to source card S1 (Q1–Q9 below), and no external *result* is used as a premise, so this is not a (D): nothing is misused. A journal would require the citation to be supplied. |
| Proof plan, "Its `L²` norm averages to `d` over `μ`" | C (typo-level) | It is the *squared* `L²` norm that averages to `d` (Lemma 3: `∫K(v,v)dμ = ∫‖k_v‖²dμ = d`). The lemma itself is stated correctly. |
| Corollary B, "the space of quadratic forms replaced by the affine space `G := {X ↦ α + ⟨A,X⟩ ...}`" | C (typo-level) | `G` is a *linear* subspace of `L²(μ)` whose elements are affine functions, not an affine space; the proof uses it as a linear subspace (Riesz representer, orthonormal basis), which is correct. |
| Corollary B, "`c ∈ (0,1]`" | C (routine) | The verbatim transfer of the Theorem's proof yields the sharper `c ∈ [(n²+1)^{-1/2}, 1]`; only the weaker `(0,1]` is recorded. Weaker, not wrong; nothing downstream needs the lower bound. |

Independent-verification note (not a defect, recorded for the editor): I attempted to confirm the numbering and page of Question 8.1 directly against arXiv:1701.06321 by web fetch. The PDF text layer is not parseable by the fetch tool and the HTML mirror returned demonstrably unreliable extractions (one fetch returned "Question 8.1: Can one improve the exponent √n further?", another claimed Section 8 contains no numbered questions at all — mutually inconsistent, and inconsistent with the same page's own rendering of Definition 2.2 and Theorem 2.3, which *do* match card S1). I therefore rely on card S1, which the harness supplies as the authority for this source and which covers every quotation in the artifact verbatim. This is **not** a class (E): a card is supplied for every cited item, and in any case no source item is a premise of the Theorem — the source is load-bearing only for the framing claim "this answers Question 8.1", exactly as the artifact itself states.

### STEP LOG ###

**Step 0 — completeness gate.** The file ends with `### END OF ARTIFACT [T-1] ###`. No sentence, display, or proof terminates mid-stream; every lemma has a `∎`. Gate passed; full verification performed.

**Class-A check first: what is actually proved, and does it match the Contract?**

What the artifact proves (my words): *For every `n ≥ 1` and every Borel probability measure `μ` on `S^{n-1}` there are a point `v_0 ∈ supp μ`, a scalar `c ∈ [(n(n+1)/2)^{-1/2}, 1]`, and a function `r : S^{n-1} → R` which is the restriction of a single quadratic form (hence bounded and Borel) with `E_μ|r| = 1`, `E_μ[|r| log|r|] ≤ log K(v_0,v_0) ≤ log(n(n+1)/2)`, and `E_μ[r(v)vv^T] = c·v_0v_0^T` exactly.* Corollary: since `log(n(n+1)/2) ≤ (2/(eδ))n^δ` for all `n ≥ 1` and all `δ > 0`, the Contract statement holds with `C(ε,δ) = 2/(eδ)`.

Diff against the Contract, item by item:
- Quantifier order. Contract: `∀ε ∀δ ∃C ∀n ∀μ ∃r ∃L`. Artifact: `C := 2/(eδ)` is fixed before `n` and `μ` are introduced and depends on neither; `r`, `L` are produced after `n, μ`. **Order matches**; no swap, no "for all sufficiently large `n`" (the bound is proved for every `n ≥ 1`, with `n = 1` treated separately and correctly: `log 1 = 0`).
- Conclusion strength. The Contract asks for `‖E[r vv^T] − L‖_F ≤ ε‖L‖_F`; the artifact delivers `= 0`, and `O(log n)` entropy instead of `O(n^δ)`. Strengthened conclusion, not weakened. `ε` is genuinely eliminated, so the Contract's Reading Convention 1 ("track the dependence of `C` on `ε`") is satisfied in the strongest way.
- Hypotheses. No hypothesis is added to `μ` (arbitrary Borel probability measure; the finitely-supported case is *not* assumed — Lemma 1(b) is precisely the device that avoids an approximation argument, and the artifact says so).
- Definition of the witness. Contract requires `r` Borel, `μ`-integrable, `E_μ|r| = 1`, `E_μ[|r|log|r|] ≤ K`. Artifact's `r = c·q_{Q_k}` is a genuine everywhere-defined function (not merely an `L²` class), continuous hence Borel, bounded hence integrable; `E_μ|r| = 1` exactly, not approximately. Contract's Regularity convention 2 respected ("no further regularity added": `r` is only claimed bounded Borel, which is a property, not an extra hypothesis).
- `L`. Contract requires rank one and nonzero, not symmetric/PSD. Artifact returns `L = c v_0v_0^T` with `c > 0`: rank one ✓, nonzero ✓ (`‖L‖_F = c ≥ (n(n+1)/2)^{-1/2} > 0`), and additionally PSD — a bonus, not a weakening.
- Ambiguous terms read the easy way? The two candidates are (i) "entropy cost", read as `E_μ[|r|log|r|]` exactly as the Contract's Definition specifies (not as `Δ_KL` of a signed object, not as `(E|r|)·log|r|`), and (ii) "nonzero rank one `L`", where the degenerate reading would be to drive `M := E[r vv^T]` to `0` and exploit the relative error. Remark A is precisely the check that this loophole does not exist and is not used. **No class-A drift.**

**Lemma 1 — verified.**
- (P1) `μ(supp μ) = 1`: complement of support is a union of open null sets; `S^{n-1}` second countable ⟹ Lindelöf ⟹ countable subcover ⟹ null. Accepted.
- (P2) is the definition of support. Accepted.
- (a) `Q ↦ q_Q` linear ⟹ `F_μ` is the image of `Sym_n`, so `d ≤ dim Sym_n = n(n+1)/2`. `|v^TQv| ≤ ‖Q‖_op ≤ ‖Q‖_F` on `S^{n-1}` (for symmetric `Q`, `‖Q‖_op = max|λ_i| ≤ (Σλ_i²)^{1/2} = ‖Q‖_F`). `μ` probability ⟹ `L^∞ ⊆ L²`. Accepted.
- (b) `h := q_{Q−Q'}` continuous with `μ({h ≠ 0}) = 0`; if `h(v_0) ≠ 0` at `v_0 ∈ supp μ` then `U := {|h| > |h(v_0)|/2}` is open (continuity), contains `v_0` (since `|h(v_0)| > |h(v_0)|/2`), so `μ(U) > 0` by (P2), yet `U ⊆ {h ≠ 0}` is null. Contradiction. This is the load-bearing measure-theoretic point — evaluation is *not* well defined on `L²` classes in general, and the artifact isolates and settles exactly the needed statement (agreement of quadratic-form representatives at *every* support point, not merely a.e.). Linearity of `ev_v` inherited from linearity of `Q ↦ q_Q(v)`, with the correct caveat that `ev_v` is defined *through quadratic-form representatives*. Accepted. I specifically checked that no later step evaluates a class at a point outside `supp μ` except through a fixed quadratic representative (Remark C(i) at `−v_0` — see below).
- (c) `F_μ` finite-dimensional subspace of `L²(μ)` hence closed hence a Hilbert space; `ev_v` linear on a finite-dimensional inner-product space hence bounded; Riesz gives a unique `k_v ∈ F_μ`. `K(v,v) := ‖k_v‖² = ev_v(k_v)` by taking `f = k_v`. Accepted. (Degeneracy check: `ev_v ≠ 0` always, since `ev_v([1]) = 1` by (d), so `k_v ≠ 0` and `d ≥ 1`; the artifact never needs to say this, but no step silently assumes `d ≥ 1` without it being true.)
- (d) `q_I(v) = ‖v‖² = 1` on the sphere; `q_{E_{ab}}(v) = v^T(e_ae_b^T + e_be_a^T)v/2 = v_av_b`. Verified by direct expansion. Accepted. This is the whole engine: the sphere constraint makes the constant a *homogeneous* quadratic form.

**Lemma 2 — verified.** Absolute convergence: `k` bounded by 1(a), `|v_av_b| ≤ 1`. (i) `E_μ[k v_av_b] = ⟨k, g_{ab}⟩ = ev_{v_0}(g_{ab}) = (v_0)_a(v_0)_b` for every `a,b`, hence the matrix identity `E_μ[k vv^T] = v_0v_0^T`. (ii) `E_μ[k] = ⟨k,[1]⟩ = q_I(v_0) = 1`; then `E_μ|k| ≥ |E_μk| = 1` and `K = E_μ[k²] ≥ (E_μ|k|)² ≥ 1`; `E_μ[k] = 1 ≠ 0 ⟹ k ≠ 0`. (iii) `E_μ|k| ≤ (E_μk²)^{1/2}` (Cauchy–Schwarz against `1`), finite by 1(a). All accepted. The "structural note" (that (ii) is the trace of (i), using `tr(v_0v_0^T) = ‖v_0‖² = 1`) is correct and non-circular — (ii) is also proved directly.

I stress-tested (i) independently on three instances not in the artifact, because it is the claim on which everything rests. (α) `μ` uniform on `S^{n-1}`: the kernel is `k_{v_0}(v) = 1 + (n(n+2)/2)(⟨v_0,v⟩² − 1/n)`; using `E[v_1^4] = 3/(n(n+2))`, `E[v_1²v_2²] = 1/(n(n+2))` I obtain `E[k vv^T] = I/n + v_0v_0^T + I/2 − ((n+2)/2n)I = v_0v_0^T` exactly, and `k_{v_0}(v_0) = n(n+1)/2 = d`. (β) `μ` uniform on a great circle in `R^3`: `k = 1 + 2cos φ`, `E[k] = 1`, `E[k²] = 3 = d`, `E[k vv^T] = diag(1,0,0) = v_0v_0^T`. (γ) `μ` uniform on the scaled Boolean cube `{±1}^n/√n`, the natural hard instance: `F_μ = span{1, v_iv_j (i<j)}`, `d = 1 + n(n−1)/2`, `k(x) = 1 + Σ_{i<j}x_ix_j = 1 + (S²−n)/2`; direct computation gives `E[k v_iv_j] = 1/n = (v_0)_i(v_0)_j` for `i ≠ j` and `E[k v_i²] = E[k]/n = 1/n` for `i = j`, so `E[k vv^T] = J/n = v_0v_0^T` exactly, `k` is genuinely signed (`k < 0` when `S² < n − 2`), and `E[r²] = K/(E|k|)² = Θ(1)`. Lemma 2(i) survives all three.

**Lemma 3 — verified, including the point where "average" must be converted to "existence in `supp μ`".** `k_v = Σ_j ⟨k_v,f_j⟩f_j = Σ_j ev_v(f_j)f_j = Σ_j q_j(v)f_j`, so `K(v,v) = Σ_j q_j(v)² =: g(v)` on `supp μ`; `g` is a polynomial (Borel, bounded on the compact sphere), agrees with `K(·,·)` up to the null set `S^{n-1}∖supp μ` by (P1), and `∫g dμ = Σ_j ∫q_j²dμ = Σ_j‖f_j‖² = d` (finite sum, no interchange issue). Independence of the choices on `supp μ` follows from uniqueness in 1(c). The averaging-to-existence step is done correctly and is the one place a sloppy proof would fail: it is *not* enough to have `K(v,v) ≤ d` `μ`-a.e., because `k_{v_0}` is only defined for `v_0 ∈ supp μ`. The artifact gets `μ(A) > 0` for `A = {g ≤ d}` (else `h = g − d > 0` `μ`-a.e. with `∫h dμ = 0`, contradiction) and then `A ∩ supp μ ≠ ∅` from (P1). Accepted. The parenthetical identification `∫K dμ = tr P = dim F_μ` (mean reciprocal Christoffel function) is correct.

**Lemma 4 — verified.** `φ(t) = t log t` satisfies `φ ≥ −1/e` on `[0,∞)` and `φ(t) ≤ t²` (for `t ≥ 1` from `log t ≤ t`; for `t < 1` from `φ ≤ 0`), so `|φ(|r|)| ≤ max(1/e, r²) ∈ L¹(μ)` and the entropy is a finite real number — the finiteness is *needed* and is supplied. `ν := |r|dμ` is a probability measure since `E_μ|r| = 1`; `ν({r = 0}) = ∫_{\{r=0\}}|r|dμ = 0`, so `log|r|` is `ν`-a.e. finite and `E_ν[log|r|] = E_μ[|r|log|r|]` with the `0log0 = 0` convention correctly matched to a `ν`-null set; `E_ν|r| = E_μ[r²] < ∞`, so Jensen for the concave `log` applies and gives `E_μ[|r|log|r|] ≤ log E_μ[r²]`. Accepted. The sharpness remark is also correct: strict concavity of `log` forces `|r|` constant `ν`-a.s., i.e. `|r| ∈ {0,c}` `μ`-a.e., whence `c = 1/μ(A)` for `A = {|r| = c}` and both sides equal `log(1/μ(A))`.

**Theorem — verified.** `v_0` from Lemma 3; `E_μ|k| ∈ [1, K^{1/2}]` from Lemma 2 gives `c := 1/E_μ|k| ∈ [K^{-1/2},1] ⊆ [(n(n+1)/2)^{-1/2},1]` (division legitimate: `E_μ|k| ≥ 1 > 0`). `r := c·q_{Q_k}` is bounded Borel, `E_μ|r| = 1`. `E_μ[r vv^T] = c v_0v_0^T = L`, `‖L‖_F = c‖v_0v_0^T‖_F = c‖v_0‖² = c > 0`, rank one, nonzero, error `0 ≤ ε‖L‖_F` for every `ε > 0`. Entropy: `E_μ[|r|log|r|] ≤ log E_μ[r²] = log(c²K) = log(K/(E_μ|k|)²) ≤ log K ≤ log(n(n+1)/2)`, using `E_μ|k| ≥ 1`. Every clause of the Definition and of the Contract's conclusion is checked; nothing is constructed and left unverified. Accepted.

**Corollary — verified.** `n = 1`: `n(n+1)/2 = 1`, entropy bound `0 ≤ C·1`. `n ≥ 2` (indeed all `n ≥ 1`): `n(n+1)/2 ≤ n²` since `n + 1 ≤ 2n`, so `log(n(n+1)/2) ≤ 2 log n`. `f(x) = (log x)/x^δ` has `f'(x) = x^{-δ-1}(1 − δ log x)`, maximised at `x = e^{1/δ}` with value `1/(eδ)`; hence `2 log n ≤ (2/(eδ))n^δ`. So `C(ε,δ) = 2/(eδ)` works, uniformly in `n` and `μ` and free of `ε`. The one asserted "standard fact" (`max_{x>0}(log x)/x^δ = 1/(eδ)`) is stated with its maximiser, so the number is checkable from the artifact alone. Accepted.

**Remark A — verified line by line** (I gave it theorem-level scrutiny, as instructed). `L = tuw^T` with unit `u,w`, `t > 0` parametrises all nonzero rank-one matrices and `‖L‖_F = t`. `‖M − L‖_F² = ‖M‖_F² − 2t⟨M,uw^T⟩ + t²` with `⟨M,uw^T⟩ = u^TMw` (using `M = M^T`) and `‖uw^T‖_F = 1`; requirement `(1−ε²)t² − 2(u^TMw)t + ‖M‖_F² ≤ 0` with `1 − ε² > 0` since `ε ∈ (0,1)`. The coefficient of `t` is the only place `u,w` enter, monotonically, and `max_{‖u‖=‖w‖=1}u^TMw = s_1(M) = |λ_1|` is attained (SVD; singular values of a symmetric matrix are `|λ_i|`). Solvability in `t` ⟺ discriminant `4s_1² − 4(1−ε²)‖M‖_F² ≥ 0` ⟺ `λ_1² ≥ (1−ε²)Σ_iλ_i²` ⟺ `Σ_{i≥2}λ_i² ≤ (ε²/(1−ε²))λ_1²`; and when real, both roots are positive (product `‖M‖_F²/(1−ε²) > 0` needs `M ≠ 0`, sum `2s_1/(1−ε²) > 0`), so an admissible `t > 0` exists exactly then. The `M = 0` bullet needs `ε < 1`, which is assumed. The "iff" is genuine in both directions. Accepted; and note the Theorem's own output has `M = L ≠ 0`, consistent with the Remark.

**Corollary B — verified modulo the (C) above.** `X = {rank 1, ‖X‖_F = 1} = {ab^T : ‖a‖ = ‖b‖ = 1}` (rescale `a,b`; `‖ab^T‖_F = ‖a‖‖b‖`), compact. `G = span{1, X_{ab}}` has `dim ≤ n²+1`, elements continuous and bounded on `X` (`|α + ⟨A,X⟩| ≤ |α| + ‖A‖_F`), so 1(a),(b),(c),(d) transfer; Lemma 2(i) becomes `E_μ[k(X)X_{ab}] = (X_0)_{ab}`, 2(ii) `E_μ[k] = 1` from `1 ∈ G`, Lemma 3 gives `X_0 ∈ supp μ` with `K(X_0,X_0) ≤ d ≤ n²+1`, Lemma 4 unchanged, so `c := 1/E_μ|k| ∈ (0,1]`, `E_μ[rX] = cX_0` exactly rank one and nonzero, entropy `≤ log(n²+1)`. Accepted. Scope is honestly declared: the unit-Frobenius normalisation is *narrower* than Theorem 2.3's "any distribution over rank one `n×n` matrices" (card Q2), and the artifact says so and points at card Q8 rather than claiming the general case.

**Remark C — checked against card S1, item by item.**
- C(i) first leg: the gloss "*deficient* reweightings ... are probability distributions and hence nonnegative" is exactly what card Q9 licenses (Definition 2.2 defines `k`-deficient only for a probability distribution `μ′`), and the card itself flags this reading. So the tightness sentence (Q5) is about a nonnegative object and is not contradicted by a signed construction. Correct use of the card; no (D).
- C(i) second leg: "`the answer to this question is No if one does not allow negative reweighting functions` (Q1) is an existential statement over `μ`" — correct: negating the universally-quantified Question yields a witness measure, so exhibiting a signed solution for all `μ` is consistent.
- C(i) appended derivation: `k ≥ 0` `μ`-a.e. ⟹ `μ′ := k dμ` is a probability measure (Lemma 2(ii)) with `E_{μ′}[vv^T] = v_0v_0^T` (Lemma 2(i)); for unit `w ⊥ v_0`, `E_{μ′}⟨v,w⟩² = w^Tv_0v_0^Tw = 0` ⟹ `⟨v,w⟩ = 0` `μ′`-a.s.; over an orthonormal basis of `v_0^⊥` plus `‖v‖ = 1` ⟹ `μ′({±v_0}) = 1`. Then `1 = μ′({±v_0}) = k(v_0)μ({v_0}) + k(−v_0)μ({−v_0}) = K(v_0,v_0)μ({±v_0})`, using `k(v_0) = ev_{v_0}(k) = K(v_0,v_0)` (Lemma 1(c)) and evenness of quadratic forms for `k(−v_0) = k(v_0)`. I checked the one soft spot here: `−v_0` need not be in `supp μ`, but the computation only ever uses the *fixed quadratic representative* as the density of `μ′`, and if `μ({−v_0}) > 0` then `−v_0` is an atom, hence in `supp μ`, where Lemma 1(b) makes all representatives agree; if `μ({−v_0}) = 0` the term drops. The step is sound as written. Finally `μ({±v_0}) = 1/K ≥ 2/(n(n+1))` and `r′ = 1_{\{±v_0\}}/μ({±v_0})` is a nonnegative reweighting with `E_μ[r′vv^T] = v_0v_0^T` (using `(−v_0)(−v_0)^T = v_0v_0^T`) and cost `log(1/μ({±v_0})) = log K ≤ log(n(n+1)/2)`. Accepted, and the Remark's self-containment claim ("nothing is taken on the authority of a document outside this artifact") holds.
- C(ii): card Q4 does state the submatrix dictionary for *flat* `μ′`, and the constructed `r` is a quadratic form with no flatness guarantee, so declining the log-rank route is a correct reading of the card, not a dodge. Theorem 2.4 (Q3) is indeed never touched.
- C(iii): card Q1's "if appropriately extended to pseudo-distributions" does grammatically attach to the running-time clause; and every use of `supp μ` (Lemma 1(b),(c), Lemma 2, Lemma 3, Theorem) does presume an honest measure. Correct.
- Consistency cross-check with the Contract's "Known boundary" (`δ = 1/2` tight for `r ≥ 0`): the artifact's `O(log n)` bound does not collapse that, because it is achieved by a signed `r` in general; the artifact's own numerical check (`k = (3,−1,1,1)`) and my Boolean-cube computation (γ above) confirm that `k` is genuinely signed on natural instances, so Remark C(i) is not vacuously true. No contradiction.

**DEPENDENCIES — checked.** All eight source quotations in the artifact are character-for-character the card's Q1, Q2, Q9, Q4, Q5, Q6, Q7, Q8. No external *result* is used as a premise of the Theorem; the four "external mathematics" items (finite-dimensional Riesz, Cauchy–Schwarz, Jensen for concave `log`, `‖Q‖_op ≤ ‖Q‖_F` / `μ(supp μ) = 1` / `max(log x)/x^δ`) are all restated in the form used and all standard. The artifact's self-assessment "None of the source material is load-bearing for the Theorem" is accurate.

**NUMERICAL CHECK — recomputed from scratch and confirmed.** For `μ` uniform on `(1,0),(0,1),(1,1)/√2,(1,−1)/√2`, the map `Q = [[a,b],[b,c]] ↦ (a, c, (a+c)/2+b, (a+c)/2−b)` is injective with image `{x : x_1+x_2 = x_3+x_4}`, so `d = 3` and `F_μ` is exactly that 3-space. Solving `(1/4)k − e_1 ⊥ F_μ` with `k ∈ F_μ` gives `k = (3,−1,1,1)` — matches. `E_μ[k] = 1`, `K = 12/4 = 3 = d`, `E_μ[k vv^T] = (1/4)[[4,0],[0,0]] = v_1v_1^T` (I re-added the four rank-one matrices), `E_μ|k| = 3/2`, `c = 2/3`, `r = (2,−2/3,2/3,2/3)`, `E_μ|r| = 1`, entropy `= (1/2)log(4/3) = 0.14384`, `E_μ[r²] = 4/3` with `log(4/3) = 0.28768`, `log 3 = 1.09861`, and `c²K = 4/3` consistent. Every displayed digit is right, and `r` is indeed signed.

**Common failure patterns probed, with result.** Quantifier order: correct (`C` before `n, μ`; `v_0` chosen *after* `μ`). Dropped "sufficiently large `n`": none — `n = 1` handled explicitly. Union/probability bound over an unbounded index: none used; the only sum is the finite `Σ_{j≤d}`. Worst-case vs expected: the single averaging step (Lemma 3) is correctly converted into existence of a point *inside* `supp μ`, which is what `k_{v_0}` requires. Asymptotic standing in for a needed constant: no — the constant `2/(eδ)` is explicit and derived. Lost reduction/hybrid factor: no reduction or hybrid. Adversary/oracle overreach: n/a. Simulation claimed not shown: n/a. Unshown independence: none assumed. Limit/sum–integral/expectation interchange: only finite sums, plus absolute convergence explicitly checked in Lemma 2 and integrability in Lemma 4. Negligible/measure-zero exception treated as empty: the opposite — (P1), Lemma 1(b) and Lemma 3 are precisely the places this could go wrong and each is handled. Missing induction base: no induction. Object constructed but not shown to have every required property: I checked `r` (Borel, integrable, `E|r| = 1`, entropy bound, everywhere-defined representative) and `L` (rank one, nonzero, error) against the Contract's Definition and conclusion clause by clause; all present. Degenerate/vacuous reading: excluded by Remark A (the relative error cannot be gamed by `M = 0`) and by the exhibited signed examples (the construction is not secretly the nonnegative one).

### SOURCE REQUEST ###
none — card S1 covers every external quotation and I confirmed each artifact quotation against it verbatim; no (D) or (E) was raised. (For the editor's convenience only, not as a blocking request: a proper bibliographic citation of the source inside the artifact, i.e. Barak–Kothari–Steurer, *Quantum entanglement, sum of squares, and the log rank conjecture*, STOC 2017 / arXiv:1701.06321v2, would close the class-(C) attribution item above. My own attempt to re-verify the "Question 8.1, p. 19" label directly from arXiv failed for tooling reasons — the PDF text layer was unparseable and the HTML mirror returned mutually contradictory extractions — so the card remains the authority, as the harness intends.)
