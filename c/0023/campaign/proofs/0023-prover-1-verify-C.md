---
id: 0023-prover-1-verify-C
agent: verifier
model: claude-sonnet-5
cycle: 2
---

# Verification report: 0023-prover-1 (rung I01, cycle 2, pass C)

## 1. FINAL VERDICT

CLEAN — the artifact completes (`### END OF ARTIFACT 0023-prover-1 ###` present), proves exactly the I01 statement (no drift), and every load-bearing lemma (1–6 and the Theorem) checks out step by step under hand-verification, including the two places most likely to hide an error (the inline hypercube isoperimetry induction, Lemma 3, and the linearity/index-enlargement step in Lemma 6); I find no Class A/B/C/D/E defect worth requiring a change, only one cosmetic (PEDANTIC-grade) note on a non-load-bearing remark's citation framing.

## 2. FINDINGS TABLE

| Location (quoted) | Class | Explanation |
|---|---|---|
| §10, Remark 10.1: "This is the NegRow/PosCol pattern of [ACC22] Claim B.3 — card S1 — reproved inline" | (D)-adjacent, non-load-bearing, PEDANTIC | The construction actually used (uniform distribution over $d$ single-point row/column indicators, each contributing average influence exactly $1/(2d)$) is a distinct, simpler object from ACC22's printed NegRow/PosCol (which are *sums* of ANDs, per card S1 item S1.c, with *strict* inequality `RelInf < 1/(2n)`, singleton **point-distribution** supports rather than uniform distributions over $d$ functions). The label "the NegRow/PosCol pattern ... reproved inline" slightly overstates the correspondence. Immaterial: the remark is explicitly flagged non-load-bearing, is fully self-proved without invoking the card's content, and the card is cited only as provenance context, not as justification (checked, per the artifact's own §12 dependency statement and the "the card is context, not justification" caveat in the remark itself). No repair needed; noted for completeness only. |
| (none elsewhere) | — | No other candidate defects found after full step-by-step re-derivation of Lemmas 1–6 and the Theorem (see log below). |

No Class A (statement drift), B (critical error), C (justification gap), or E (unverifiable) findings.

## 3. STEP-BY-STEP LOG

**STEP 0 (completeness).** File ends with `### END OF ARTIFACT 0023-prover-1 ###`; no mid-sentence/mid-equation truncation observed. PASS.

**Class A check (statement drift).** The artifact's §0 restates the I01 statement verbatim, identical to `intermediates/I01-spread-junta.md` (same class $\mathcal C^{junta}_d$, same quantifier order $\exists c_1,c_2,\delta,\ \forall d,N,\mathbf F,\mathbf G,\ \exists f,g,x$, same per-coordinate-average-over-$[N]$ influence hypothesis, same exact non-vanishing conclusion). It supplies explicit witnesses $c_1=1/3,c_2=1,\delta(d)=1/(3d)$ and checks admissibility ($\delta(d)\in(0,1]$ for $d\ge1$, $\delta(d)\ge c_1d^{-c_2}$ with equality). $\delta$ depends only on $d$, never $N$ — verified directly from the formula and from Lemma 6's proof, where the window-budget bound $|J_a|,|K_b|\le d$ is the *only* place $N$-dependence could sneak in, and it doesn't. The hypothesis used throughout is genuinely the **average** $\mathbb E_{\mathbf F}[\mathrm{Inf}_i]$ (Lemma 6's $\delta_F:=\max_i\mathbb E_{\mathbf F}[\mathrm{Inf}_i]$), never a max-over-support substitute — I specifically traced Lemma 6's derivation of $\Sigma_1\le\delta_F\sum_b q_b|K_b|$ to confirm the expectation is taken correctly (see below). No drift found.

**Lemma 1 (influence = edge count).** Step 1 (Parseval/orthonormality of $\chi_T$) re-derived and correct. Step 2 (flip identity, $\mathrm{Inf}_i(f)=\tfrac14\mathbb E[(f(x)-f(x^{\oplus i}))^2]$) correct, standard. Step 3 (edge count for a normalized indicator): recomputed $(f(x)-f(x^{\oplus i}))^2$ case split and $\mathbb E_x[\cdot]=2|\partial_iA|/|A|$ — arithmetic checks. Step 4 (junta lift, $i\notin J$ trivial; $i\in J$: $|\partial_iA|=b_i(P)2^{N-|J|}$, $|A|=|P|2^{N-|J|}$) — checked the bijection between window boundary edges and lifted $N$-cube edges; correct. Final formula $\mathrm{Inf}_i(f)=b_i(P)/(2|P|)$ verified by direct substitution. **Tag: NONE** (fully inline, no external dependency).

**Lemma 2 (disjointness = projection disjointness).** Both directions checked by direct construction/contrapositive; the $(\Rightarrow)$ direction's explicit witness $x$ (patching $p$ on $J$, $q$ on $K\setminus J$, $+1$ elsewhere) verified to lie in both $A$ and $B$ using $p_S=q_S$. Degenerate case $S=\emptyset$ (one-point cube, both projections equal it, must intersect) correctly forces $A\cap B\ne\emptyset$ whenever $S=\emptyset$ — this is the fact Lemma 5 leans on to get $S\ne\emptyset$ for free. **Tag: NONE.**

**Lemma 3 (hypercube isoperimetry, $|\partial_EW|\ge|W|\log_2(2^n/|W|)$), the flagged non-elementary ingredient.** Re-derived the full induction independently:
- Base case $n=0$: trivial, both sides 0. Correct.
- Exact boundary decomposition $|\partial_EW|=|\partial_EW_+|+|\partial_EW_-|+|W_+\triangle W_-|$ — verified as an *exact* identity (facet edges vs. direction-$n$ edges partition all edges), then correctly weakened via $|W_+\triangle W_-|\ge a-b$.
- Case $b=0$: recomputed $a\log_2(2^{n-1}/a)+a = a\log_2(2^n/a)$ algebraically — exact match, no slack lost.
- Case $b\ge1$: reduced the required inequality to $g(a):=(a+b)\log_2(a+b)-a\log_2a-b\log_2b-2b\ge0$ for real $a\ge b>0$ — I independently re-derived this reduction from (3.3) and it matches the artifact's algebra exactly. Checked $g(b)=0$ directly ($2b\log_2(2b)-2b\log_2b-2b=2b\cdot1-2b=0$). Checked $g'(a)=\log_2\frac{a+b}a>0$ via the standard derivative $\frac{d}{da}[a\log_2a]=\log_2a+1/\ln2$ — correct, so $g$ increasing on $[b,\infty)$, hence $g(a)\ge0$ for real $a\ge b$, applied at the integer values. No gap.
- Numerically stress-tested the lemma independently on $n=2,3$ configurations not in the artifact's own sanity checks (e.g. an $n=3$ path-shaped $W=\{(+,+,+),(+,+,-),(+,-,-)\}$, giving $|\partial_EW|=5\ge3\log_2(8/3)\approx4.245$ ✓; an $n=3$ independent set of even-weight vertices, $12\ge4$ ✓) — all consistent, no violation found.
- Equality case (Remark 5.1, subcubes) matches the induction's own tightness at $b=0$ and at $a=b$. **Tag: NONE** (proved from scratch, genuinely a correct — if non-tight-in-general, tight-on-subcubes — entropy-type edge-isoperimetric bound; this is the artifact's own claimed "isoperimetry-type lemma proved by induction" and it survives close reading of base case, induction step, and equality case as directed).

**Lemma 4 (projection-density payment).** $S=\emptyset$ case: $0\ge0$, correct ($\nu_P(\emptyset)=1$). Main case: fiber-count function $w$, identities $\sum_uw(u)=|P|$, $w(u)\ge1\iff u\in\pi_S(P)$ checked. Step 1 ($\sum_{i\in S}b_i(P)\ge \mathrm{TV}_S(w)$): re-derived the bijection between window $i$-edges and $S$-cube $i$-edges $\times$ fibers, and the bound $|V_u\triangle V_{u'}|\ge|w(u)-w(u')|$ (standard symmetric-difference-vs-cardinality-difference inequality) — correct. Step 2 (layer-cake identity $|c-c'|=\sum_{t\ge1}|\mathbf 1[c\ge t]-\mathbf 1[c'\ge t]|$ for nonneg integers, and the resulting $\mathrm{TV}_S(w)=\sum_t|\partial_EL_t|$) — standard identity, checked, and the finite-sum exchange is unconditionally valid. Step 3 (applying Lemma 3 to each nonempty $L_t\subseteq\pi_S(P)$, then monotonicity of $\log_2$ with nonneg prefactor to replace $|L_t|$ by $|\pi_S(P)|$ in the denominator, then $\sum_t|L_t|=|P|$) — checked each inequality direction; correctly uses $|L_t|\le|\pi_S(P)|\Rightarrow 2^s/|L_t|\ge2^s/|\pi_S(P)|$, log monotone, multiply by nonneg $|L_t|$. Assembly and division by $2|P|>0$, substitution of Lemma 1 for $i\in S\subseteq J$ — correct. **Tag: NONE** (built purely from Lemmas 1 and 3).

**Lemma 5 (per-pair payment $\ge1$).** $S\ne\emptyset$ forced by Lemma 2. $\nu_P(S)+\nu_Q(S)\le1$ from disjoint subsets of the $S$-cube — correct; AM–GM to $\nu_P\nu_Q\le1/4$ — correct. Lemma 4 applied twice (once to $(J,P,S)$, once to $(K,Q,S)$ — both legitimate since $S\subseteq J$ and $S\subseteq K$), added, and $\log_2(1/(\nu_P\nu_Q))\ge\log_24=2$ used to get the final $\ge1$. Re-derived on a probe case not in the artifact (single-point $P$ on a $2$-window vs. its 3-point complement $Q$ on the same window: computed $\mathrm{Inf}$ values directly via Lemma 1, got total payment $4/3>1$, consistent, no violation) as an informal negation check — found no case approaching or breaking the bound below the tight $=1$ case of Remark 7.1. **Tag: NONE.**

**Lemma 6 (master count) — the linearity/index-set step most likely to hide an "average vs. worst-case" bug.** This is the step I scrutinized hardest, per the harness's flag on counting/linearity-of-expectation steps. Re-derived from scratch:
- $\sum_{a,b}p_aq_b\,X_{ab}\ge\sum_{a,b}p_aq_b=1$ where $X_{ab}\ge1$ pointwise by Lemma 5 — valid nonneg-weighted sum of pointwise lower bounds.
- Splitting into $\Sigma_1+\Sigma_2\ge1$: correct, finite sums.
- The crux: bounding $\Sigma_1=\sum_{a,b}p_aq_b\sum_{i\in S_{ab}}\mathrm{Inf}_i(f_a)$. I checked explicitly that the artifact enlarges $S_{ab}$ to the **partner's** window $K_b$ (not to $J_a$, the sample's own window) before summing over $a$. This is the correct — and non-obvious — choice: enlarging to $J_a$ instead would require bounding $\sum_{i\in J_a}\mathrm{Inf}_i(f_a)$ *pointwise per sample $a$* by $\delta_F|J_a|$, which is FALSE in general (a single sample's influence can exceed the distributional average $\delta_F$; this is exactly the "worst-case vs. expected" failure pattern flagged in the harness). By enlarging to $K_b$ instead, $i$ ranges over a set independent of $a$ before the inner sum over $a$ is taken, so $\sum_a p_a\,\mathrm{Inf}_i(f_a)=\mathbb E_{\mathbf F}[\mathrm{Inf}_i]$ is a genuine, legitimate expectation, correctly bounded by $\delta_F$ termwise, and only then summed over $i\in K_b$ (giving $\delta_F|K_b|$) and over $b$ with weight $q_b$ (giving $\delta_F\sum_bq_b|K_b|$). I independently re-derived this chain of (in)equalities and it is correct; the finite-sum reordering used is valid. Symmetric treatment of $\Sigma_2$ via $J_a$ checked identically. Assembling: $\delta_F\sum_bq_b|K_b|+\delta_G\sum_ap_a|J_a|\ge\Sigma_1+\Sigma_2\ge1$ (correct direction: upper bounds on $\Sigma_1,\Sigma_2$ combined with a lower bound on their sum). Then $\sum_bq_b|K_b|\le d$, $\sum_ap_a|J_a|\le d$ (weighted averages of quantities $\le d$) give $(\delta_F+\delta_G)d\ge1$, and $\delta_F+\delta_G\le2\max(\delta_F,\delta_G)$ gives $\max(\delta_F,\delta_G)\ge1/(2d)$. All directions of every inequality checked; no union bound over an unbounded index is used anywhere (the only "sum over $N$" that occurs is bounded, term-by-term, by $d$ via the window-size hypothesis, never by $N$). **Tag: NONE.**

**Theorem.** Admissibility of $(c_1,c_2,\delta)$ checked directly. Contradiction argument: "conclusion fails" $\iff$ "every pair of supports disjoint" (checked via the sign structure of $f(x)g(x)$ for normalized indicators) $\iff$ Lemma 6's incompatibility hypothesis. Lemma 6 gives $\max(\delta_F,\delta_G)\ge1/(2d)$; the influence hypothesis gives $\delta_F,\delta_G\le\delta(d)=1/(3d)$; $1/(2d)>1/(3d)$ for all $d\ge1$ — direct numeric contradiction, correct. **Tag: NONE.**

**Remark 9.1 ($0\in\mathbb N$ edge case) and Remark 3.1 (unit norm / degree $\le d$ "automatic" clauses).** Both re-checked independently (the degree argument via the flip-and-negate-$\chi_T$ trick for $i\in T\setminus J$ is a standard and correct argument); both explicitly non-load-bearing, consistent with the rest.

**Remark 10.1 (tightness, non-load-bearing).** Re-derived independently: single-point row/column patterns on a $d\times d$ grid, $N=d^2$; $\mathrm{Inf}_i(f_r)=1/2$ on $J_r$ via Lemma 1 (single-point pattern, $b_i=1$); average over $\mathbf F$ uniform on $\{f_r\}$ gives $\mathbb E_{\mathbf F}[\mathrm{Inf}_{(r',c')}]=1/(2d)$ exactly (only the $r=r'$ term contributes) — recomputed and confirmed. No common nonvanishing point (row/column patterns conflict at their crossing coordinate) — confirmed. This independently reproduces (via a distinct, simpler construction) the Contract's K2 threshold $1/(2d)$, and it is fully consistent with — indeed numerically tight against — Lemma 6's bound $\max(\delta_F,\delta_G)\ge1/(2d)$, which is a strong internal cross-check that Lemma 6 is not subtly too weak or too strong. See Findings Table for the one cosmetic note on this remark's citation framing.

**STEP 0.5 NEGATION CHECK.** For the two lemmas most load-bearing and least "obviously true" on sight — Lemma 3 (isoperimetry) and Lemma 6 (master count) — I attempted to construct or motivate a counterexample to the *negation* in a separate context (i.e., tried to find $W$ violating Lemma 3, and tried to find an incompatible $(\mathbf F,\mathbf G)$ violating Lemma 6's conclusion). No such object was found; every probe (several $n=2,3$ hypercube subsets for Lemma 3; the single-point-vs-complement pair and the grid construction for Lemma 5/6) satisfied the lemma with the expected slack, and the extremal cases found (subcubes for Lemma 3; the grid construction of Remark 10.1 for Lemma 6) hit the claimed bounds with equality, exactly as the artifact predicts. I did not find both a lemma and its negation independently "provable" in different local contexts — no CONTRADICTION triggered.

## 4. SOURCE REQUEST

None. The artifact is self-contained; the only card reference (S1, in Remark 10.1) is explicitly non-load-bearing context, not relied upon for justification, and both cards (S1, S2) were read as part of this review and are consistent with the Contract's framing.

### END OF ARTIFACT 0023-prover-1-verify-C ###
