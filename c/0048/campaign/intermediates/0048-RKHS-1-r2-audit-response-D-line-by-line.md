MODEL: fable

### VERDICT ###
STATUS: DEFECTS
The main Theorem, its proof (Lemmas 1–4), the Corollary with C(ε,δ) = 2/(eδ), and the worked numerical example all verify exactly under independent recomputation, but Remark C.1 contains a false quantitative claim: the negativity region of the uniform-measure kernel has μ-measure tending to ≈ 0.6827, not 1 − o(1); the defect is confined to a remark the artifact itself marks as not load-bearing for the Theorem, but it is a false mathematical assertion as written and so forces DEFECTS.

### FINDINGS ###
| quoted location | class A-E | explanation |
|---|---|---|
| Remark C.1: "the kernel at any `v_0` is `k(v) = (n(n+2)/2)⟨v,v_0⟩² − n/2`, negative on `{⟨v,v_0⟩² < 1/(n+2)}`, a set of `μ`-measure `1 − o(1)`" | B | The kernel formula and the negativity region are correct (I re-derived both from E[v_a v_b v_c v_d] = (δ_abδ_cd + δ_acδ_bd + δ_adδ_bc)/(n(n+2)): writing Q = a·v₀v₀ᵀ + bI, the reproducing condition forces a = n(n+2)/2, b = −n/2). The measure claim is false. For uniform μ, n⟨v,v₀⟩² converges in distribution to χ²₁ and the threshold n·(1/(n+2)) = n/(n+2) → 1, so μ({⟨v,v₀⟩² < 1/(n+2)}) → P(χ²₁ < 1) ≈ 0.6827, a constant strictly less than 1, not 1 − o(1). Exact small-n values confirm: n = 2 gives measure 1/3 (arc where cos²θ < 1/4); n = 3 gives 1/√5 ≈ 0.447 (Archimedes projection, |t| < 1/√5). The measure is bounded away from 1 for every n. The qualitative point the remark needs — that k is negative on a set of positive μ-measure, so the construction is genuinely signed on the uniform instance — is true, but the stated quantification "1 − o(1)" is wrong as written, and the parenthetical "(exact check recorded in the review of this artifact; not re-derived here)" defers to a document not supplied, so nothing in the artifact supports it. This invalidates that sentence only. It is logically independent of the Theorem, the Corollary, Corollary B, and Remark A, all of which I verified separately. |
| Theorem proof: "So `r` is admissible for Definition `def:signed`" and Corollary: "Conjecture `conj:main` holds" | C | Dangling label references (`def:signed`, `conj:main`) to a labeling scheme not present in the artifact; the referents are unambiguous (the GOAL's definition and claim). Cosmetic; a competent reader fills this without effort, and it does not by itself force DEFECTS. |

### STEP LOG ###
- STEP 0: `### END OF ARTIFACT [P-1] ###` present; artifact complete. PASS.
- Statement drift: artifact proves, for every n and every Borel probability μ on S^{n-1}, a signed reweighting r (E_μ|r| = 1, Borel, bounded, hence μ-integrable) with entropy ≤ log(n(n+1)/2) and a nonzero rank-one L with E_μ[r vvᵀ] = L exactly; Corollary converts to entropy ≤ (2/(eδ))n^δ. This implies the Contract verbatim (exact equality ⇒ error 0 ≤ ε‖L‖_F for every ε; C = 2/(eδ) finite, depends only on δ, not on n or μ; quantifier order matches Contract convention 1; no regularity beyond Borel added, per convention 2; L not required symmetric — the constructed L is PSD, which is allowed, per convention 3). NO DRIFT.
- Lemma 1(a): |vᵀQv| ≤ ‖Q‖_op ≤ ‖Q‖_F on the unit sphere; L^∞(μ) ⊆ L²(μ) for probability μ; d ≤ dim Sym_n = n(n+1)/2. Recomputed. PASS.
- Lemma 1 (P1): complement of supp μ is the union of all open null sets; second countability makes it a countable union of null base elements, hence null. PASS.
- Lemma 1(b): h = q_{Q−Q'} continuous and μ-a.e. zero; if h(v₀) ≠ 0 for v₀ ∈ supp μ, the open set {|h| > |h(v₀)|/2} contains v₀, has positive measure by definition of support, yet is contained in the null set {h ≠ 0}. Contradiction. Well-definedness and linearity of ev_v follow. PASS.
- Lemma 1(c): F_μ finite-dimensional ⇒ Hilbert; Riesz representer exists and is unique; k_v = Σ_j ev_v(f_j)f_j correct for any orthonormal basis; K(v,v) = ev_v(k_v) by taking f = k_v. PASS.
- Lemma 1(d): q_I(v) = ‖v‖² = 1 on the sphere; vᵀE_ab v = v_a v_b with E_ab = (e_ae_bᵀ + e_be_aᵀ)/2. Recomputed: (v_av_b + v_bv_a)/2 = v_av_b. PASS.
- Lemma 2(i): E_μ[k v_a v_b] = ⟨k, g_ab⟩ = ev_{v₀}(g_ab) = (v₀)_a(v₀)_b, entrywise; absolute convergence from boundedness. PASS.
- Lemma 2(ii): E_μ[k] = ⟨k,[1]⟩ = 1; E|k| ≥ |E k| = 1; Cauchy–Schwarz E[k²] ≥ (E|k|)² ≥ 1; k ≠ 0. PASS.
- Lemma 2(iii): E[k²] = ‖k‖² = K(v₀,v₀); E|k| ≤ (E k²)^{1/2} by Cauchy–Schwarz against 1. PASS.
- Structural note (trace of (i) gives (ii)): tr E[k vvᵀ] = E[k‖v‖²] = E[k], tr(v₀v₀ᵀ) = 1. Recomputed. PASS.
- Lemma 3, identity (2): for v ∈ supp μ, ev_v(f_j) = q_j(v), so K(v,v) = Σ_j q_j(v)²; g polynomial, Borel; value on supp μ independent of choices by Riesz uniqueness. PASS.
- Lemma 3, identity (3): ∫g dμ = Σ_j ∫q_j² dμ = Σ_j ‖f_j‖² = d; g bounded on the compact sphere so integrable. Recomputed. PASS.
- Lemma 3, cheap point: if μ({g ≤ d}) = 0 then g − d > 0 μ-a.e. with zero integral, forcing g = d a.e., contradiction; μ(A) > 0 with μ(supp μ) = 1 gives A ∩ supp μ ≠ ∅. PASS.
- Lemma 4: φ(t) = t log t ≥ −1/e (minimum at t = 1/e, recomputed) and φ(t) ≤ t² for all t ≥ 0 (t ≥ 1: log t ≤ t; t < 1: φ ≤ 0); hence φ(|r|) ∈ L¹ and the entropy is finite. dν = |r|dμ is a probability measure since E_μ|r| = 1; ν({r = 0}) = 0 matches the 0 log 0 convention; E_ν[|r|] = E_μ[r²]; Jensen for concave log gives E_ν[log|r|] ≤ log E_μ[r²]. All recomputed. PASS.
- Lemma 4 sharpness note: Jensen equality for strictly concave log iff |r| is ν-a.e. constant, i.e. |r| ∈ {0,c} μ-a.e.; then c = 1/μ({|r| = c}) and entropy = log(1/μ({|r| = c})). Recomputed. PASS (not load-bearing).
- Theorem, normalisation: c = 1/E|k| ∈ [K(v₀,v₀)^{-1/2}, 1] ⊆ [(n(n+1)/2)^{-1/2}, 1] from 1 ≤ E|k| ≤ K^{1/2} and K ≤ d ≤ n(n+1)/2. Recomputed. PASS.
- Theorem, rank one: E[r vvᵀ] = c·v₀v₀ᵀ = L; ‖L‖_F = c‖v₀‖² = c > 0; L rank one, nonzero, PSD; Frobenius error exactly 0 ≤ ε‖L‖_F for every ε > 0. PASS.
- Theorem, entropy: E[|r|log|r|] ≤ log E[r²] = log(c²K) = log(K/(E|k|)²) ≤ log K ≤ log(n(n+1)/2), using E|k| ≥ 1 and Lemma 3. Recomputed. PASS.
- Corollary, n = 1: log(1·2/2) = 0 ≤ C·1^δ. PASS.
- Corollary, n ≥ 2: n+1 ≤ 2n so n(n+1)/2 ≤ n², log(n(n+1)/2) ≤ 2 log n. Calculus claim: d/dx[(log x)x^{-δ}] = x^{-δ-1}(1 − δ log x) vanishes at x = e^{1/δ}, maximum value (1/δ)e^{-1} = 1/(eδ) (natural log, consistent with the artifact's numerics). Hence 2 log n ≤ (2/(eδ))n^δ and C(ε,δ) = 2/(eδ), independent of ε. Recomputed. PASS. (Note: the Contract fixes no logarithm base; the artifact consistently uses natural log throughout, including the numerical check, and existence of a finite C is base-independent.)
- Remark A: ‖M − tuwᵀ‖² = ‖M‖² − 2t·uᵀMw + t²; requirement ⇔ (1−ε²)t² − 2(uᵀMw)t + ‖M‖² ≤ 0; optimum uᵀMw = s₁(M) = |λ₁| for symmetric M; real root iff s₁² ≥ (1−ε²)Σλᵢ², which rearranges (recomputed) to Σ_{i≥2}λᵢ² ≤ (ε²/(1−ε²))λ₁²; for M ≠ 0 both roots positive (product ‖M‖²/(1−ε²) > 0, sum 2s₁/(1−ε²) > 0); M = 0 case correct. PASS (not load-bearing).
- Corollary B: X = {abᵀ : ‖a‖ = ‖b‖ = 1} is the continuous image of a compact set, hence compact; G = {X ↦ α + ⟨A,X⟩} has dimension ≤ n² + 1, contains the constant (α = 1, A = 0) and every coordinate (α = 0, A = e_ae_bᵀ); its elements are continuous (Lemma 1(b) transfers) and bounded on X (Lemma 1(a) transfers); Lemma 3 analogue gives ∫K dμ ≤ n² + 1, whence entropy ≤ log(n² + 1). PASS.
- Remark C.1, kernel formula for uniform μ: independently re-derived. With E[v_av_bv_cv_d] = (δ_abδ_cd + δ_acδ_bd + δ_adδ_bc)/(n(n+2)), the reproducing condition E[(vᵀQv)(vᵀAv)] = v₀ᵀAv₀ for all A ∈ Sym_n reads (tr Q)I + 2Q = n(n+2)v₀v₀ᵀ; solving Q = a·v₀v₀ᵀ + bI gives a = n(n+2)/2, b = −n/2, i.e. k(v) = (n(n+2)/2)⟨v,v₀⟩² − n/2. Formula CORRECT; negativity region {⟨v,v₀⟩² < 1/(n+2)} CORRECT.
- Remark C.1, measure of the negativity region: DEFECT (class B, see FINDINGS). μ({⟨v,v₀⟩² < 1/(n+2)}) → P(χ²₁ < 1) ≈ 0.6827 as n → ∞ (n = 2: exactly 1/3; n = 3: exactly 1/√5 ≈ 0.447), not 1 − o(1). Invalidates that sentence; the surrounding qualitative claims of C.1 — E[k] = 1 vs E|k| ≤ K^{1/2} with equality iff |k| a.e. constant, and "k negative on a positive-measure set whenever E|k| > E[k] = 1" — are correct and were verified independently; the no-conflict conclusion of C.1 survives since it needs only positive-measure negativity, but I do not repair the stated quantification.
- Remark C.2 and C.3: interpretive discussion checked against card items Q4, Q9, Q1; the "deficient ⇒ nonnegative" gloss is exactly what Q9 supports (μ′ required to be a probability distribution); the flatness caveat matches Q4; no mathematical claims requiring recomputation. PASS.
- DEPENDENCIES: all external mathematics restated and elementary (Riesz finite-dimensional, Cauchy–Schwarz, Jensen, ‖Q‖_op ≤ ‖Q‖_F, full-measure support, the max computation); all eight source quotations match card S1 verbatim (Q1–Q9); the quoted Question 8.1 matches the Contract statement with no added hypothesis. PASS.
- NUMERICAL CHECK, fully recomputed from scratch: q_Q values at the four points are (p, s, (p+s)/2 + q, (p+s)/2 − q), so F_μ = {x ∈ R⁴ : x₁ + x₂ = x₃ + x₄}, d = 3 ✓. Kernel at v₀ = v₁: solving k = 4e₁ + λ(1,1,−1,−1) with k ∈ F_μ gives λ = −1, k = (3,−1,1,1) ✓. Identity checks: E[k] = (3 − 1 + 1 + 1)/4 = 1 ✓; K = ‖k‖²_{L²} = 12/4 = 3 = d ✓; ¼(3v₁v₁ᵀ − v₂v₂ᵀ + v₃v₃ᵀ + v₄v₄ᵀ) = ¼[[4,0],[0,0]] = v₁v₁ᵀ, exactly rank one ✓. Normalisation: E|k| = 6/4 = 3/2, c = 2/3, r = (2, −2/3, 2/3, 2/3), E|r| = (2 + 2)/4 = 1 ✓. Entropy chain: E[|r|log|r|] = ¼(2 ln 2 + 2 ln(2/3)) = ½ln(4/3) = 0.14384 ✓; log E[r²] = log(c²K) = ln(4/3) = 0.28768 ✓; log 3 = 1.09861 ✓; 0.1438 ≤ 0.2877 ≤ 1.0986 ✓; r is genuinely signed ✓. ALL NUMBERS CORRECT.

### SOURCE REQUEST ###
none
