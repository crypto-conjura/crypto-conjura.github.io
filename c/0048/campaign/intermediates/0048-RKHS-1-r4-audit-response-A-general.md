MODEL: sonnet

### VERDICT ###
STATUS: CLEAN
The artifact proves exactly the Contract's statement (via a stronger, exact-equality construction with entropy O(log n)), and every lemma, the Corollary's algebra, the auxiliary Remarks, and the citations check out under line-by-line and independent numerical re-verification.

### FINDINGS ###

| quoted location | class A-E | explanation |
|---|---|---|
| "Run Lemmas 1–4 verbatim with `S^{n-1}` replaced by `X`..." (Corollary B proof) | (C), routine, does not force DEFECTS | The proof compresses the four-lemma argument into one paragraph by name-checking which two properties of the function space are used. I checked independently that Lemma 3's averaging argument (⟨supp∩{g≤d}≠∅⟩) uses nothing sphere-specific — only finite dimension, continuity/Borel-ness of the kernel-square function, and the probability-measure vanishing-integral argument — so the compression is legitimate. A competent reader reconstructs it without effort; flagged per the instructions' own allowance for routine gaps. |

No defect of class A (statement drift), B (broken step), D (citation misuse), or load-bearing E (unverifiable) was found. Details of the verification are in the Step Log below, including places where I actively tried to break the argument.

### STEP LOG ###

**Step 0 / completeness.** File ends with `### END OF ARTIFACT [W-1] ###`; no truncation.

**Statement drift check.** The artifact's GOAL section restates the Contract's claim verbatim in substance (same quantifier order ∀ε∀δ∃C∀n∀μ∃r∃L, same Definition of signed reweighting with the same 0log0 convention, same relative-Frobenius accuracy target, same "not required nonnegative" reading of r, same "not required symmetric/PSD" reading of L). The artifact then proves something *stronger* than required — exact equality (Frobenius error 0, valid for every ε>0) at entropy cost ≤ log(n(n+1)/2) = O(log n), and derives the Contract's O(n^δ) claim as an immediate corollary with C(ε,δ)=2/(eδ) via the elementary bound log x ≤ x^δ/(eδ) (checked: max_{x>0} log(x)/x^δ = 1/(eδ) at x=e^{1/δ}, correct calculus). A strictly stronger construction correctly instantiates the weaker existential Contract claim; no drift, no weakening, no smuggled hypothesis.

**Lemma 1(a).** F_μ = image of Sym_n under Q↦q_Q, so d≤n(n+1)/2 — correct (linear image of an n(n+1)/2-dim space). ‖q_Q‖_∞≤‖Q‖_F via ‖Q‖_op≤‖Q‖_F (standard, correctly listed under Dependencies). Accepted.

**Lemma 1(b).** Standard topological support argument (open superlevel set around a point of nonzero continuous h contains a positive-measure neighborhood, but lies inside the μ-null set {h≠0} — contradiction). Verified the logic of the open set U:={|h|>|h(v0)|/2} being open, containing v0, and contained in {h≠0}; correct. This is the crux step making the "generic finite-support" argument work for arbitrary Borel μ; checked carefully since the artifact itself flags it as the one place where the Borel case differs from the finite case. Accepted.

**Lemma 1(c),(d).** Riesz representation on a finite-dim inner product space (standard, correctly cited) and the sphere identities q_I≡1, q_{E_ab}(v)=v_av_b (direct computation, verified: v^T E_ab v = (v_av_b+v_bv_a)/2 = v_av_b). Accepted.

**Lemma 2(i).** E_μ[k v_av_b] = ⟨k,g_ab⟩ = ev_{v0}(g_ab) = (v0)_a(v0)_b via the reproducing property and Lemma 1(d); assembling entrywise gives E_μ[k vv^T]=v0v0^T. Verified this is not an artifact of a degenerate representative choice, since Lemma 1(b) makes ev_{v0} representative-independent on supp μ. Accepted.

**Lemma 2(ii),(iii).** E_μ[k]=1 via [1]∈F_μ (reproducing property again); E_μ|k|≥1 by triangle inequality; K(v0,v0)=E_μ[k²]≥(E_μ|k|)²≥1 by Cauchy–Schwarz against the constant function 1 (μ a probability measure); k≠0 since its mean is 1. Upper bound E_μ|k|≤K(v0,v0)^{1/2} also Cauchy–Schwarz. All checked, correct.

**Lemma 3.** ∫K(v,v)dμ=d is the standard "reciprocal Christoffel function" identity; re-derived it myself via k_v=Σ_j ev_v(f_j)f_j (checked by verifying ⟨Σ_j ev_v(f_j)f_j, f_m⟩=ev_v(f_m) for each basis vector, hence for all f∈F_μ by linearity — matches the defining property of k_v, so uniqueness in Lemma 1(c) forces this formula). Parseval sum ∫g dμ=Σ‖f_j‖²=d correct. The existence-of-cheap-point argument (μ(A)=0 ⟹ g−d>0 a.e. with zero integral, contradiction; hence μ(A)>0, hence A∩suppμ≠∅ by (P1)) is a standard "average implies existence of a point at most the average" argument, correctly executed with the appropriate care about Borel measurability of the polynomial g and its relation to K(·,·) only being canonically defined on supp μ.

**Lemma 4.** φ(t)=t log t bounded above by t² (checked: for t≥1, log t≤t so φ(t)≤t·t=t²; for t<1, φ(t)≤0≤t²); ν:=|r|dμ is a probability measure since E_μ|r|=1; E_ν[log|r|]=E_μ[|r|log|r|] correctly matches the 0log0 convention on the ν-null set {r=0}; Jensen for concave log against ν gives the stated bound. Standard and correctly executed.

**Proof of the Theorem.** c:=1/E_μ|k| correctly bracketed in [K(v0,v0)^{-1/2},1] from Lemma 2(ii,iii); r:=ck has E_μ|r|=1 by direct computation; E_μ[r vv^T]=c·v0v0^T=L exactly (Frobenius error literally 0, hence ≤ε‖L‖_F for every ε>0); L is rank one/nonzero/PSD with ‖L‖_F=c>0, all verified directly. Entropy chain E_μ[|r|log|r|]≤log E_μ[r²]=log(c²K(v0,v0))=log(K(v0,v0)/(E_μ|k|)²)≤log K(v0,v0)≤log(n(n+1)/2) verified term by term (uses E_μ|k|≥1 and Lemma 3's bound). Accepted — this is the strongest and most surprising step in the artifact, so I additionally **independently re-derived it from scratch** for a non-trivial test case not present in the artifact: μ = uniform (rotation-invariant) measure on the full sphere S^{n-1}. By the rotational-symmetry ansatz k_{v0}(v)=a+bt² (t=⟨v,v0⟩), using the standard 4th-moment tensor of the uniform sphere measure (E[v_iv_jv_kv_l]=(δ_{ij}δ_{kl}+δ_{ik}δ_{jl}+δ_{il}δ_{jk})/(n(n+2))), I solved for a=−n/2, b=n(n+2)/2 and confirmed independently that E_μ[k_{v0} vv^T]=v0v0^T exactly, E_μ[k_{v0}]=1, and K(v0,v0)=k_{v0}(v0)=n(n+1)/2 — all matching the artifact's claims exactly with no discrepancy. This closes off the concern that the exact-equality / O(log n) result is "too good to be true" for a well-known open question: it is a genuine, checkable identity, and the artifact's own Remark C(ii)–(iii) correctly explains why it does not automatically improve the log-rank conjecture bound (the construction gives a *signed*, non-flat reweighting, whereas the log-rank consequence requires a *flat, nonnegative* combinatorial rectangle — a materially different and harder object that this construction does not supply).

**Corollary (main).** Algebra n(n+1)/2≤n² for n≥2, log(n(n+1)/2)≤2log n, and 2log n≤(2/(eδ))n^δ via the AM–GM-type bound — all verified by direct computation; n=1 case (log 1=0) handled separately and correctly. Accepted.

**Remark A.** Full eigenvalue characterization of when a rank-one L achieves relative error ≤ε against a symmetric M; re-derived the quadratic (1−ε²)t²−2s_1(M)t+‖M‖_F²≤0 independently and verified the "both roots positive" claim via sum=2s_1/(1−ε²)>0, product=‖M‖_F²/(1−ε²)>0 (using s_1=|λ_1|, the top singular value, not λ_1 itself — the artifact's use of s_1(M) here is consistent throughout despite the earlier λ notation). Correct; explicitly marked non-load-bearing.

**Corollary B.** Re-derivation strategy verified (see Findings table above for the one (C) noted). The key structural point — that the target moment is E[r(X)·X] (linear in X), hence the correct analog of "F_μ" is the affine function space G rather than a quadratic-form space — is correctly identified and is the right generalization mechanism (unlike a naive verbatim substitution, which would have been wrong). Accepted.

**Remark C (scope).** (i) Verified the "no contradiction" argument for the nonnegative-tightness claim: it is a genuinely non-circular observation — when the construction's r is already nonnegative, r itself is a witness for the nonnegative sub-problem at the same cost, so such μ isn't a counterexample to the source's tightness claim; this is correctly limited to that case and not overclaimed. The "more precisely, μ({±v0})=1/K(v0,v0)≥2/(n(n+1))" refinement is proved in the closing derivation of the Remark; I checked this derivation step by step: E_{μ'}⟨v,w⟩²=0 for w⊥v0 forces v∈{±v0} μ'-a.s. (finite union over an ONB of v0^⊥, correctly finite so no unbounded-union issue), the evenness of the quadratic-form representative correctly gives k(−v0)=k(v0)=K(v0,v0) regardless of whether −v0 is itself in supp μ, and the final atom-based reweighting r′ with entropy log(1/μ({±v0}))=log K(v0,v0)≤log(n(n+1)/2) checks out via direct computation (matches the "conditioning on an event" sharpness case flagged in Lemma 4). (ii) and (iii) are accurate, appropriately modest disclaimers (no flatness guarantee is supplied, so no log-rank consequence follows; the argument uses supp μ genuinely and does not address pseudo-distributions) — verified against Q4's flat-distribution definition and against the fact that every use of "supp μ" in Lemmas 1–3 and the Theorem indeed presumes an honest (not pseudo-) distribution.

**Dependencies / citations.** Cross-checked every verbatim quotation (Question 8.1, Theorem 2.3, Definition 2.2, §2.2 flatness dictionary, the tightness remark, footnotes 7–8, §2.3 normalization) against the supplied source card S1-bks-q81-card.md word for word — all match exactly. The "external mathematics" listed (Riesz representation, Cauchy–Schwarz, Jensen, ‖Q‖_op≤‖Q‖_F, μ(suppμ)=1, the max(log x)/x^δ identity) are all standard and correctly restated, with no misapplication found (checked each against the hypotheses actually available: finite-dimensionality for Riesz, probability-measure normalization for Cauchy–Schwarz/Jensen). No citation defect. I confirmed via web search that arXiv:1701.06321 (Barak–Kothari–Steurer, STOC 2017) is the correct, existing paper matching the card's description, so the external source is not a phantom citation.

**Numerical check (artifact's own).** Recomputed independently: for the 4-point example, solved the reproducing-kernel linear system directly (not just trusting the stated answer) and obtained k=(3,−1,1,1), matching the artifact exactly; recomputed E_μ[k]=1, K(v0,v0)=3, E_μ[k vv^T]=v1v1^T exactly, c=2/3, r=(2,−2/3,2/3,2/3), E_μ|r|=1, entropy≈0.1438, log E[r²]≈0.2877, log 3≈1.0986 — every arithmetic value in the artifact's numerical check reproduces correctly to 4 decimal places.

### SOURCE REQUEST ###
none
