MODEL: sonnet

### VERDICT ###
STATUS: DEFECTS
Two extensive independent numerical/analytic refutation attempts against the main Theorem both **failed** (the construction checks out exactly), so the Theorem and Corollary stand; but Remark C.1 contains a false universal claim ("r takes both signs unless μ is a point mass" — false: any two-point antipodal support is a counterexample), which is a Class B defect, and one interpretive gloss in Remark C.1 ("deficient" = "nonnegative") is not supported by the supplied card even though it is in fact correct (verified independently against the actual paper).

### FINDINGS ###

| quoted location | class A-E | explanation |
|---|---|---|
| "Statement to be proved" / Theorem+Corollary vs. Contract | none (no drift) | The artifact proves a *strictly stronger* claim than the Contract requires: exact recovery (Frobenius error 0, hence trivially ≤ ε‖L‖_F for every ε>0) with entropy cost ≤ log(n(n+1)/2) = O(log n), and derives C(ε,δ) = 2/(eδ) independent of ε. This is a legitimate specialization of "∃ C(ε,δ)" (a constant is allowed not to depend on ε); quantifier order (∀ε∀δ∃C, then ∀n∀μ∃r,L) matches the Contract exactly, and the n=1 case and all n≥2 are handled explicitly with no silent "for sufficiently large n." No statement drift found. |
| REFUTATION ATTEMPT 1 — artifact's own numerical check (n=2, 4 points) | none — attempted refutation, failed | Independently recomputed the reproducing kernel k_{v0} at v0=(1,0) for μ uniform on {(1,0),(0,1),(1,1)/√2,(1,−1)/√2} from scratch (solving the 3×3 linear system for the Riesz representer directly, not trusting the artifact's stated answer). Obtained k=(3,−1,1,1) exactly matching the artifact. Verified E_μ[k]=1, K(v0,v0)=‖k‖²=3=d, and E_μ[k·vv^T] = v1v1^T exactly (computed the 2×2 matrix sum by hand: off-diagonal cancels to 0, diagonal collapses to (1,0;0,0)). Confirmed r=(2,−2/3,2/3,2/3) is genuinely signed, E_μ|r|=1, entropy 0.1438 ≤ log 3. No discrepancy found. |
| REFUTATION ATTEMPT 2 — continuous rotationally symmetric measure, n=2, μ = uniform on S¹ | none — attempted refutation, failed | Chose this specifically because it forces d = n(n+1)/2 = 3 exactly (the bound is tight, no slack from a non-full-dimensional F_μ), a natural place to look for the construction to fail or for the entropy bound to be violated. Using orthonormal basis {1, √2 cos2θ, √2 sin2θ} of F_μ, computed k(θ) = 1+2cos2θ analytically. Verified E_μ[k]=1 ✓, K(v0,v0)=k(0)=3=d ✓ (rotational symmetry ⇒ every point is "equally cheap," consistent with Lemma 3's average argument being tight everywhere). Computed E_μ|k| = (1/2π)∫₀^{2π}|1+2cosφ|dφ = 2/3 + (2√3)/π ≈ 1.4359 by splitting the integral at the sign-change points φ=2π/3, 4π/3. Got c ≈ 0.6964, entropy bound log(c²·3) ≈ 0.375 ≤ log 3 ≈ 1.099 ✓ (holds with room to spare, not violated). Directly verified E_μ[r·cos²θ] = c (matches L₁₁ = c), E_μ[r·cosθsinθ] = 0 (matches L₁₂ = 0), E_μ[r·sin²θ] = 0 (matches L₂₂ = 0) by explicit trigonometric-moment computation — i.e. the exact rank-one identity E_μ[r vv^T] = c·v₀v₀^T holds exactly, not approximately, for this continuous example too. No refutation found: the RKHS/reproducing-kernel machinery (Lemmas 1–4) is standard and, on these two structurally different test cases (one discrete/asymmetric, one continuous/symmetric, one saturating d = n(n+1)/2 exactly), delivers precisely what is claimed. I could not construct a dimension/measure pair for which the construction fails or exceeds its stated entropy bound. |
| REMARK C.1, "Our r takes both signs unless μ is a point mass — the kernel k has E_μ[k]=1 but E_μ|k|=K(v0,v0)^{1/2} in general, so k is negative somewhere whenever E_μ|k| > E_μ[k]." | (B) CRITICAL ERROR, non-load-bearing | The universal claim is false. Counterexample: let n=2, v0=(1,0), and let μ put mass p∈(0,1) at v0 and 1−p at −v0 (any p, not just p=1/2) — a measure with two-point antipodal support, **not** a point mass. Every quadratic form q_Q satisfies q_Q(v0)=q_Q(−v0) (since (−v)_a(−v)_b=v_av_b), so F_μ = span{[1]}, d=1. Solving Lemma 1(c)'s defining equation ⟨k,1⟩=1 in this 1-dimensional space forces k≡1 (a.e.), which is nonnegative, giving r≡1, E_μ|r| log|r| = 0, and no cancellation at all: E_μ[vv^T] = p·v0v0^T + (1−p)·v0v0^T = v0v0^T exactly, trivially. So k is *not* negative anywhere, contradicting the artifact's "unless μ is a point mass" dichotomy. The correct characterization is "unless supp μ is contained in an antipodal pair {v0,−v0}," a strictly larger class of measures than point masses. Also, the parenthetical equality "E_μ|k| = K(v0,v0)^{1/2} in general" misstates Lemma 2(iii), which only proves the inequality E_μ|k| ≤ K(v0,v0)^{1/2} (Cauchy–Schwarz), with equality iff |k| is a.e. constant — not an identity that holds "in general." This defect is confined to Remark C.1's interpretive discussion of the relationship to the source paper's nonnegative-only "No" result; it does not touch Lemmas 1–4, the Theorem, the Corollary, or Corollary B, all of which I attempted independently to refute and could not. |
| REMARK C.1: "'It can be shown that as stated, Theorem 2.3 is tight' ... concerns *deficient*, i.e. nonnegative, reweightings" | (D)/(E) minor, resolved by independent check — flagged for the record | The card (S1-bks-q81-card.md) never quotes or supplies a definition of "deficient reweighting"; it only gives the *statement* of Theorem 2.3 (Q2, using the undefined term "deficient") and the flatness dictionary for a *different* term, "flat" (Q4). Per the task's instruction to check "hypothesis by hypothesis" against the card, the equation deficient = nonnegative is **not established by the supplied card** — a referee restricted to the card alone cannot verify it (this would ordinarily be a load-bearing (E) blocking the specific sub-claim in Remark C.1, since the "no conflict" argument in C.1 depends on knowing what "deficient" means). I used the permitted web search to check the actual paper (ar5iv rendering of arXiv:1701.06321) and found Definition 2.2 there: "we say that a probability distribution μ′ is a k-deficient reweighting of μ if Δ_KL(μ′‖μ)≤k" — i.e. μ′ is explicitly required to be a probability distribution (nonnegative), confirming the artifact's gloss is factually correct. So this is not a misquotation and the conclusion drawn is accurate, but it is sourced from outside the card that was supposed to carry all load-bearing external facts; the card is incomplete for this particular (non-load-bearing-for-the-Theorem, but load-bearing-for-Remark-C.1) claim. |
| Dependencies section — Q1/Question 8.1 verbatim quote incl. "would be very interesting... No if one does not allow negative reweighting functions" | none | Matches card Q1 verbatim, word for word, including the follow-on sentence. The artifact's claim "This matches the statement of the Contract with no additional hypothesis" was checked by direct side-by-side comparison of Contract wording vs. quoted text — accurate, no smuggled hypothesis added or dropped (r not required nonnegative in either; ε,δ both universally quantified in both; L nonzero rank one in both). |
| Dependencies — Theorem 2.3 (Q2), §2.2 flatness dictionary (Q4), p.6 tightness (Q5), footnote 8 (Q7), §2.3 normalisation (Q8) | none | All verbatim quotes checked word-for-word against the card; all match exactly. Footnote 7 is quoted only partially (card's trailing sentence about "rectangles whose distance to being rank one ... is only a small constant or 1/polylog(n)" is omitted) but footnote 7 is not used anywhere in the artifact's load-bearing reasoning (it appears only for context in the Dependencies list), so the omission is not a fidelity violation. |
| REMARK C.2 ("log rank consequence does not follow") | none | Conclusion drawn ("Our r is not flat and not nonnegative, so it names no submatrix, and Theorem 2.4's dual form is untouched") stays strictly within what Q4's quoted flatness dictionary supports — it requires flatness for the submatrix correspondence, the artifact's r is not flat, and the artifact draws no stronger conclusion than "the dictionary doesn't apply." No overreach. |
| REMARK C.3 (sos/pseudo-distribution obstruction) | none | This is presented, correctly, as the artifact's own mathematical observation (the failure of Lemma 1(b)/ev_{v0} for pseudo-distributions), not attributed to the source paper. The only quoted fragment ("if appropriately extended to pseudo-distributions") is used merely to note that the paper itself flags this as needing extra work, which is a fair reading of that clause — no claim is made about what the source paper's text does or doesn't prove about pseudo-distributions beyond what is quoted. |
| Lemmas 1–4, Theorem, Corollary, Corollary B (core mathematics) | none found | Re-derived Lemma 1(a)(b)(c), Lemma 2(i)(ii)(iii), Lemma 3's averaging argument, and Lemma 4's entropy≤log(second moment) bound from scratch conceptually and numerically (see refutation attempts above); all check out. The max_{x>0}(log x)/x^δ = 1/(eδ) calculus fact used in the Corollary was independently re-derived and confirmed. |

### STEP LOG ###
- Step 0 completeness: artifact ends with "### END OF ARTIFACT [M-1] ###"; complete. Accepted.
- Statement-drift check: Theorem/Corollary proves a claim at least as strong as the Contract in every quantifier and every bound; no weakening, no reordering, no asymptotic-for-exact substitution. Accepted.
- Lemma 1(a)(b)(c)(d): standard finite-dimensional RKHS facts (dimension bound, well-definedness of point evaluation via continuity + null-set argument, Riesz representer, membership of 1 and coordinate products in F_μ). Accepted after independent spot check.
- Lemma 2(i)(ii)(iii): reproducing identities; re-derived and numerically confirmed on two test measures (see Findings). Accepted.
- Lemma 3: averaging/pigeonhole argument for a cheap base point; measure-theoretic step (μ(A)>0 ⟹ A∩supp μ≠∅) verified correct via monotonicity + μ(supp μ)=1. Accepted.
- Lemma 4: entropy ≤ log(second moment) via change of measure + Jensen; re-derived independently, matches. Accepted.
- Main Theorem construction (v0, k, c, r, L): tested against two independently computed concrete examples (discrete 4-point measure on S¹, continuous uniform measure on S¹); both times the construction delivered exact rank-one recovery within the claimed entropy bound. REFUTATION ATTEMPT FAILED on both tries — reporting as such per instructions, not as a defect.
- Corollary (C(ε,δ)=2/(eδ)): calculus step re-derived independently (max of log(x)/x^δ), case n=1 and n≥2 both checked. Accepted.
- Remark A: not load-bearing per artifact's own note; not deeply re-verified beyond a plausibility check of the underlying quadratic formula, since it plays no role in the Theorem/Corollary and my mandate was to focus on refutation of the Theorem and on citation fidelity.
- Remark B / Corollary B: generalization to general rank-one matrices via an affine function space; the two properties used (contains constants, contains coordinate functions) are checked present; not exhaustively re-verified numerically (secondary to the Contract's claim, and explicitly non-load-bearing).
- Remark C.1: found the antipodal-pair counterexample described above — logged as Class B, non-load-bearing.
- Source-fidelity pass: every verbatim quotation in the artifact (Dependencies section and inline in Remark C) checked word-for-word against cards/S1-bks-q81-card.md; all matched except the unquoted, un-carded equivalence "deficient = nonnegative," which I additionally checked against the actual paper via web search (ar5iv rendering) and found to be correct as stated by the artifact, though not verifiable from the card alone.

### SOURCE REQUEST ###
For the record (not blocking, since I was able to independently resolve it via web search and it checked out true): the card cards/S1-bks-q81-card.md should include the verbatim definition of "k-deficient reweighting" (paper's Definition 2.2, roughly p. 5: "we say that a probability distribution μ′ is a k-deficient reweighting of μ if Δ_KL(μ′‖μ)≤k") so that Remark C.1's claim "deficient, i.e. nonnegative" is verifiable from the supplied card rather than from outside knowledge. This does not affect the Theorem, Corollary, or Corollary B, none of which rely on this term.
