---
title: "Example Conjecture"
resolution: "<span class='cj-badge cj-badge-open'>○ Open</span>"
verification: "<span class='cj-badge cj-badge-ai'>AI-reviewed</span>"
categories: [Lattices]
---

::: {.callout-note appearance="simple"}
This is a template page. Replace this content with the real conjecture, then delete this note. Copy this whole folder (`index.qmd`, `latex/`, `pdf/`, `sessions/`, `lean/`) as the starting point for a new conjecture.
:::

## Status

- *Resolution:* <span class='cj-badge cj-badge-open'>○ Open</span> — or <span class='cj-badge cj-badge-partial'>◐ Partially Solved</span> / <span class='cj-badge cj-badge-solved'>● Solved</span>
- *Verification:* <span class='cj-badge cj-badge-ai'>AI-reviewed</span> — or <span class='cj-badge cj-badge-human'>Human-reviewed</span> / <span class='cj-badge cj-badge-formal'>✓ Formally checked</span>
- *Best known result:* —
- *Related work:* —

::: {.callout-note appearance="simple" title="Badge reference"}
Set `resolution:` and `verification:` in the frontmatter to exactly one of the following (used verbatim as the listing-table cell value too), and mirror the same value in the two bullets above:

- `resolution`: `<span class='cj-badge cj-badge-open'>○ Open</span>` / `<span class='cj-badge cj-badge-partial'>◐ Partially Solved</span>` / `<span class='cj-badge cj-badge-solved'>● Solved</span>`
- `verification`: `<span class='cj-badge cj-badge-ai'>AI-reviewed</span>` / `<span class='cj-badge cj-badge-human'>Human-reviewed</span>` / `<span class='cj-badge cj-badge-formal'>✓ Formally checked</span>` — "Formally checked" means a completed Lean proof, not just today's statement-only `lean/Statement.lean`.
:::

::: {.panel-tabset}

## Statement

[View PDF](pdf/main.pdf) · [Download LaTeX source](latex/main.tex)

State the conjecture precisely here, using standard math notation, e.g. $\mathsf{SIS}_{n,m,q,\beta}$ is hard for parameters ...

## Proof

This conjecture is open — no proof exists yet.

Once (an attempt at) a proof exists, add its own `.tex` source and compiled PDF (e.g. `latex/proof.tex` / `pdf/proof.pdf`) and link them here, following the same convention as the statement.

## Formal Artifact

[Lean statement](lean/Statement.lean)

Only the conjecture's *statement* has been formalized so far (as a Lean theorem ending in `sorry` — see `lean/Statement.lean`), not a proof. Once a proof exists, replace the `sorry` (or add a separate `lean/Proof.lean`) and update this tab.

:::

## Source material

If this conjecture was imported from an existing paper, the original LaTeX source and compiled PDF live alongside this page in `latex/` and `pdf/` (see the Statement tab above).

## Research sessions

Dumps of prompt/agent sessions attempting this conjecture are kept in `sessions/` for provenance. They are not rendered as part of the site.
