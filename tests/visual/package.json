{
  "name": "conjura-visual-regression",
  "private": true,
  "description": "Dev-only Playwright harness for the CSS consistency refactor. Not part of the site build; serves the pre-rendered _site/ statically and diffs screenshots. Safe to delete once the refactor is verified.",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
