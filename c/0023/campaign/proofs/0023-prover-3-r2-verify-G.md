---
id: 0023-prover-3-r2-verify-G
agent: verifier
model: claude-sonnet-5
cycle: 3
---

## FINAL VERDICT

PARTIAL / UPHELD-WITH-ONE-DEFECT: the artifact is complete (ends with its END-OF-ARTIFACT marker), its core mathematics (T1–T9, the two exact witness tables, CAP I, CAP II, the non-canonicity theorem, the group generalisation, and the §7.4 certificate-minimality arithmetic, including the 5/32-vs-1/8 recomputation) all check out exactly by hand; but the title and the lead sentence of VERDICT clause (iv) assert, unqualified, that "P5 escapes both caps," when §7.4/[G5] establish this only for a *minimum*-certificate variant of P5 and explicitly leave open whether P5's actually-declared technique (*minimal* certificates, per its own citation to `0023-strategist-2`) escapes at all — a class (A) steering-document overclaim at the artifact's most-read level, adequately hedged only deeper in the body.

## FINDINGS TABLE

| # | Location (quoted) | Class | Explanation |
|---|---|---|---|
| 1 | Title: "…and the **two live plans escape both caps**"; VERDICT clause (iv), first sentence: "**P1 and P5 ESCAPE both caps — YES, both, by exact computation.**" | **A** (steering-document statement drift) | §7.4 itself states: "P5's plan uses *minimal* certificates with an averaged inequality…; §7.4 evaluates *minimum*-size certificate selections," and closes with "P5 is therefore not capped… — **for minimum-size selections**; see [G5] for the unrestricted minimal-certificate variant." [G5] then states plainly: "whether such a [minimal] selection keeps $\mathbb E\lvert W(A_k)\rvert=\mathrm{poly}(d)$ … is **not determined here**. The escape claim of §7.4 is asserted for minimum-size selections only." Witness (a) is shown to admit minimal certificates of size $2^{d-1}$ (exponential, not poly(d)) at some points, so it is not merely a technicality: CAP I(b)/(c)'s hypothesis test (Remark 2.2's expected window size) is genuinely open for P5's real technique. The title and the opening clause of the VERDICT — the two places a busy reader is likeliest to stop — assert the escape for "P5" without this qualifier; only later in the *same* clause does "the minimum-certificate window (P5)" appear. A researcher who reads only the headline could conclude P5 (as strategist-2 actually defines it) is cleared of both caps; the artifact's own body says the opposite is still open. This is exactly the "what it appears to advise vs. what it proves" gap the review brief asks to hunt for, and it sits directly beside the one *new* gap the revision introduced (G5), which is precisely why it survived: G5 undermines the escape claim it sits beside, at the headline level, even though the body (§7.4, the U4 changelog entry, [G5]) discloses the restriction correctly and repeatedly. P1's escape claim has no analogous problem — §7.1 evaluates exactly P1's declared window ("any maximum-degree monomial support," matching strategist-2 table V1 verbatim), so the asymmetry is specific to P5. |
| 2 | T6 (CAP I) and T8 (CAP II), plus Remark 4.2$'$ | none (verified correct) | The newly-added nonemptiness hypotheses ($\lvert W(A_k)\rvert+\lvert W(B_k)\rvert\ge1$ for CAP I; $W(C)\ne\emptyset$ for CAP II) are genuinely discharged, not merely asserted, at every place a cap is actually invoked: automatically for CAP I(a)–(c) (via $\mathrm{Rel}(A_k)\ne\emptyset$, $c>0$, $2^{\varepsilon d}\ge1$), and by explicit computation for CAP II ($W^\theta_{\mathrm{hvy}}(C)=W_{\mathrm{Forced}}(C)=[d]\ne\emptyset$, Remark 4.2$'$/4.2(b)). Both caps retain non-trivial numeric content ($(d+1)2^{-d-1}$ and $\tfrac1{2(2^d-1)}$, both strictly positive for finite $d$) after the predecessor's degenerate "$V_W=0$" branch was removed (changelog U2). No defect found here; this is the artifact's strongest technical improvement over the predecessor. |
| 3 | §7.4, the $u$-minimisation and the 5/32-vs-1/8 arithmetic | none (independently recomputed by hand, no Bash tool available in this session) | Recomputed exactly: $g(u)=k-u+2^u$ is minimised at $u\in\{0,1\}$ with value $k+1=d$ (confirms "minimum certificate size $=d$, attained exactly at $u\in\{0,1\}$"). The per-selection payment $h(u)=\tfrac{k-u}4+2^u2^{-k-1}$ satisfies $h(0)-h(1)=\tfrac14-2^{-k-1}\ge0$ for $k\ge1$, so the worst (minimising) selection is $u_A=u_B=1$, giving combined excess $\tfrac12-2^{-k}$ over the all-$u=0$ selection — exactly as claimed. The resulting ratio $\bigl[\tfrac{k-1}2+2^{1-k}\bigr]/(2(k+1))$ evaluates to $\tfrac14,\tfrac16,\tfrac5{32},\tfrac{13}{80}$ at $k=1,2,3,4$ — matching the artifact exactly, including the reported minimum $5/32$ at $k=3$ (verified: at $k=5$ the ratio rises again to $33/192=11/64\approx0.172$, confirming $k=3$ is indeed the global minimum among small $k$, not merely a local one, consistent with the artifact's claim that $5/32>1/8$). The algebraic equivalence "$\ge\tfrac18 \iff k\ge3-2^{3-k}$" was independently re-derived and confirmed true for all $k\ge1$. §7.5's average $\tfrac k4+4^{-k}$ was independently re-derived from $\mathbb E[\tfrac12\lvert\{t:a_t\ne a_t'\}\rvert]+\mathbb E[2^{-k}\mathbf1\{a=a'\}]=\tfrac k4+4^{-k}$ using independent-uniform addresses — exact match. No arithmetic defect found. |
| 4 | §5.3's boxed conditional sentence on R4, and the surrounding "R4 is NOT condemned" paragraph | none (verified sound, both directions) | The inequality $(d+1)2^{-d-1}<c\,e^{-d^\alpha}$ for large $d$ (α<1) was independently confirmed (exponential decay beats sub-exponential decay), so the conditional is validly derived. The "not condemned" half is also sound: by 5.2 (monotonicity), a cap proved on a witness inside $\mathcal C^{\mathrm{ind}}_d$ propagates only *upward* to larger classes, never downward, and §6.1 correctly uses this to keep frozen R1 uncondemned (witness (a) lies outside R1's smaller junta class; witness (b) lies inside it but fails CAP II's own-heaviness hypothesis there, landing in T9's non-exponential branch, independently re-verified: the exact value at $W=W_{\mathrm{rel}}$ on witness (b) is $\tfrac14+\tfrac1{4(2^d-1)}$, consistent with "$\ge\tfrac14$"). I found nothing wrongly condemned as safe, and nothing wrongly declared safe that is actually condemned. |
| 5 | §7.2 Non-canonicity theorem (i)–(iv) | none (verified) | Independently re-derived $b(a')=b(a)\oplus c$, confirmed $\sigma_c$ is an involution fixing $A_k,B_k$ setwise, confirmed the induced action on the $2^k$ targets is transitive (translation by $c$), confirmed canonicity forces $W(A_k)\cap\{\text{targets}\}\in\{\emptyset,\text{all}\}$, and confirmed clause (iv)'s dichotomy including the size threshold $2^{d-1}$ and its CAP I consequence $(d+1)2^{-d}$. No defect. |
| 6 | §5.4 group generalisation (CAP II for all finite abelian $\mathcal Y$; CAP I pullback to even-order groups) | none (verified by independent Fourier computation) | Independently recomputed $\mathrm{Inf}_i(f_{C_{\mathcal Y}})=1-\lvert\mathcal Y\rvert^{-1}$ and $\mathrm{Inf}_i(f_{D_{\mathcal Y}})=\frac{1-\lvert\mathcal Y\rvert^{-1}}{\lvert\mathcal Y\rvert^d-1}$ from the character expansion of $\mathbf 1_{C_{\mathcal Y}}$; both match exactly, and reduce correctly to the $\mathbb Z_2$ values ($\tfrac12$ and $\tfrac1{2(2^d-1)}$) as a sanity check. The even-order pullback argument (injective dual map, equal-fibre measure preservation, disjointness preserved under preimage) is complete and correct; "abelian group has a $\mathbb Z_2$ quotient iff even order" is a standard fact, correctly used. No defect. |
| 7 | §6.1, "T9 gives $\ge\tfrac14$ there" | Pedantic | The exact value at $W=W_{\mathrm{rel}}$ on witness (b) is $\tfrac14+\tfrac1{4(2^d-1)}$, i.e. essentially exactly $\tfrac14$ (exponentially close), not merely "at least" it with meaningful slack. Correct as stated, but a reader could momentarily think this is a loose bound rather than a near-equality; the surrounding "consistent with R1's proved $\tfrac1{2d}$" sentence resolves any confusion (it is a self-check, not a claim about R1's actual constant). No rewrite required. |
| 8 | §6.2, `0023-refuter-3` §5.1's $1/(8d)$ forcing bound, used only for a "not condemned" self-check | Out of scope for this review (informational, not blocking) | This calibration line rests on a CERTIFIED-not-verified external artifact (`0023-refuter-3.md`) that this review is explicitly barred from reading. The artifact itself discloses the status honestly ("CERTIFIED (not verified)" in the dependency table) and the claim is not load-bearing for the main VERDICT (used only in §6.2's calibration self-check, "consistent with," not "equals"). Not a defect under this review's scope; flagged for completeness only. |
| 9 | Dependency table row for `0023-strategist-2.md` §2/§3.1, tagged "load-bearing? no (fidelity of the objects evaluated)" | Informational (not a defect) | The artifact's central "P1/P5" objects are *defined* by an external campaign artifact this review may not read. The artifact mitigates this by restating each window's definition in full and self-contained form inline (§7.1's "$W_{\mathrm{sh}}(A):=$ any maximum-degree monomial support," §7.4's certificate/minimal/minimum definitions), so the mathematics is independently checkable without `strategist-2`; only the *fidelity* of "this is really what P1/P5 propose" is unverifiable from within this review's permitted context, and the artifact's own table already flags this honestly. Combined with finding #1, this is the second half of why the P5-escape headline risks being over-read: even the identity of "P5" as a technique is sourced outside this file. |

## STEP-BY-STEP LOG

1. **STEP 0 (completeness).** File ends with `### END OF ARTIFACT 0023-prover-3-r2 ###` (line 1054). Complete; not TRUNCATED.
2. **STEP 0.5 (negation check).** Tested each load-bearing lemma against its negation in a separate context, looking for a hard-stop contradiction:
   - CAP I (T6) vs. "some relevance-denominated-density-$c$ $W$ exceeds $\tfrac{d+1}{c2^{d+1}}$": never asserted; every functional evaluated (W_sh, W_max) is shown to have density $2^{-\Theta(d)}$, i.e. to *fail* CAP I's hypothesis, not to violate its conclusion. No contradiction.
   - CAP II (T8) vs. "some own-heavy-at-$\theta>\tfrac1{2(2^d-1)}$, $W(C)\ne\emptyset$ functional exceeds $\tfrac1{2(2^d-1)}$": never asserted; W_max and W_sh are shown to fail the strict own-heaviness hypothesis (T7's $D$ attains the threshold *exactly*, not strictly above it). No contradiction.
   - T9's dichotomy: every named functional (W_rel, W_sh, W_max, W_Forced, certificate windows) is placed unambiguously in exactly one of the two branches ($w_D=0$ vs. $w_D\ge1$); no functional is asserted in both. No contradiction.
   - D5's degenerate $\Theta_W:=0$ convention: verified it is never triggered for any functional actually evaluated for an escape or a cap (all are shown nonempty on all of $\mathcal P_d$, except $W_{\mathrm{Forced}}$, which is correctly handled via the *restricted* $\eta^*_W$/witness-(b)-only route in §6.2/Remark 4.3, not via a spurious universal-$\eta^*$ application). No contradiction.
   - **CONCLUSION: no hard-stop CONTRADICTION found.**
3. **Class (A) drift check (Contract-level).** The artifact proves a barrier for two named technique sub-classes and explicitly disclaims proving/disproving R2 itself ("R2 remains open"); this matches its own stated scope and does not drift against the Contract or I02. Accepted.
4. **Adjudication tags for load-bearing lemmas** (per the review's tagging instruction):
   - T1 (master count): **MECH** (self-contained inline proof).
   - T2 (value theorem): **MECH**.
   - T3 (localisation ceiling / $\eta^*$): **MECH**.
   - T4 (floor: non-vanishing, relevance-intersection, influence quantum, $\eta^*$ bracket): **MECH** (T4(c) explicitly re-proves card S6a's content inline rather than citing it, per the artifact's own note "no [RESTATED] item load-bearing").
   - T5 (witness (a) exact table): **MECH + CODE** (independently hand-recomputed here; also cross-checked in the artifact by `check_witnesses.py`).
   - T6 (CAP I): **MECH** (built from T3+T5, both MECH).
   - T7 (witness (b) exact table): **MECH + CODE** (hand-recomputed here; matches `refuter-3` §4 as an attributed lead, not a citation dependency).
   - T8 (CAP II): **MECH**.
   - T9 (dichotomy): **MECH**.
   - §7.4 certificate-minimality arithmetic (5/32 bound): **MECH + CODE** (hand-recomputed here; also verified by the artifact's new `check_min_certificates.py`, exhaustive for $k\le3$).
   - §7.2 non-canonicity theorem: **MECH**.
   - §5.4 group generalisation: **MECH**.
   - Cards S1, S6, S7/S7b: **CARD**, but in every case explicitly marked non-load-bearing for the two caps or the escape claims (context/comparison/attribution only) — the artifact's own dependency table agrees.
   - No lemma load-bearing for the main VERDICT rests on a **NONE**-tagged (unsupported) claim; the sole external dependencies used in a *load-bearing* way are the I02 rung statement and `PROGRESS.md`'s rung-class definitions (both Contract-level, correctly restated inline per §5.3).
5. **Task 2 check (do the caps still have content, and are nonemptiness hypotheses discharged).** Verified positively — see Findings #2.
6. **Task 3 check (two sub-classes only; R4/R1 not falsely condemned or falsely cleared).** Verified positively in both directions — see Findings #4.
7. **Task 4 check (exact recomputation of certificate/minimality arithmetic).** Performed entirely by hand (no Bash tool was available in this session); every number reproduced exactly — see Findings #3.
8. **Task 5 check (no sentence asserts a lower bound on any $\Theta_{W_{\mathrm{sh}}}$, $\Theta_{\mathrm{cert}}$, $\Theta_{W_{\max}}$).** Scanned §7 and §8 in full: every escape sentence is phrased as "contributes no cap below X" / "hypotheses fail," with explicit standing disclaimers at the top of §7 and in the VERDICT; the one place a genuine implication "(PAY⋆) at $p=1$ forces $\eta^*(d)=1$" appears, it is correctly conditioned on an unproven hypothesis of P1's own plan (not asserted true), so it does not violate the disclaimer. **No violation found** — this is a real, verified improvement over the predecessor's central defect.
9. **Task 6 check (are the four declared gaps complete/correctly scoped, and does the new one undermine an adjacent claim).** G1–G3 are correctly scoped and do not undermine adjacent claims. **G5 (minimal-vs-minimum certificates) does undermine the adjacent escape claim** at the headline level — see Findings #1, the primary defect of this pass.

## SOURCE REQUEST

None. No load-bearing external result is unread or uncarded; the only genuinely external dependency used in a load-bearing way (the rung classes, from `PROGRESS.md`) is restated inline in §5.3 as the artifact itself intends. Findings #8–#9 are informational scope notes about this review's own permitted-context boundary, not requests for new material.

### END OF ARTIFACT 0023-prover-3-r2-verify-G ###
