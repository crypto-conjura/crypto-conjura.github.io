---
id: "0005"
problem: leftover-hash-lemma-extraction
title: "Leftover Hash Lemma Extraction Bound for Unpredictable Random-Oracle Sources, Secret Seed"
short_title: "LHL extraction, secret seed"
areas: [information-theoretic]
model: rom
form: tight-bound
assumption_class: unconditional
category: research-open
status:
  statement_informal: ai
  statement_formal: open
  statement_match: open
  proof_informal: open
  proof_review: ai
  proof_formal: open
# status_badge is generated by scripts/status_badge.py -- do not hand-edit
difficulty:
  reach: adaptation
  by: ai
  note: "The public-seed case c/0004 is proved and the secret-seed distinguisher is strictly weaker, so the route is visible; the seed's own entropy is the part the existing argument does not already handle."

status_summary: "Open: the secret-seed bound, in which the distinguisher never sees the seed, so unlike the public-seed case c/0004 the seed's own entropy is still available to the extractor."
status_updated: 2026-08-16
relations:
  - {kind: variant-of, target: "0004", note: "same setting and same source; the two differ only in whether the distinguisher receives the seed"}
lean:
  mode: none
revision: 1
sources:
  - {cite: "Farshim, Two Leftover-Hash-Lemma Conjectures (self-hosted statement source; states this secret-seed conjecture as Conjecture 1, alongside the public-seed Conjecture 2)", url: "/c/0005/latex/main.tex"}
statement_sha: "dd2c09e55a705a5ab391faffc44ccd02b334973402064fe154468bf118ffd2f9"
status_badge: "<a href='https://crypto-conjura.github.io/open-problems/status-legend/index.html#status-legend' class='cj-status-link' aria-label='Statement: AI-written, not yet formalized. Proof: open -- no attempt yet. Click for the status badge legend.'><svg class='cj-status-badge' viewBox='0 0 32 32' width='28' height='28' xmlns='http://www.w3.org/2000/svg' role='img'><circle cx='16' cy='16' r='13' fill='none' stroke-width='4' class='cj-ring-0' stroke-dasharray='1,3'/><circle cx='16' cy='16' r='8' class='cj-disc-0'/><text x='16' y='17' text-anchor='middle' dominant-baseline='central' class='cj-glyph' font-size='9'>–</text></svg></a>"
---

<!-- status:start -->
<a href='https://crypto-conjura.github.io/open-problems/status-legend/index.html#status-legend' class='cj-status-link' aria-label='Statement: AI-written, not yet formalized. Proof: open -- no attempt yet. Click for the status badge legend.'><svg class='cj-status-badge' viewBox='0 0 32 32' width='28' height='28' xmlns='http://www.w3.org/2000/svg' role='img'><circle cx='16' cy='16' r='13' fill='none' stroke-width='4' class='cj-ring-0' stroke-dasharray='1,3'/><circle cx='16' cy='16' r='8' class='cj-disc-0'/><text x='16' y='17' text-anchor='middle' dominant-baseline='central' class='cj-glyph' font-size='9'>–</text></svg></a>

*Statement: AI-written, not yet formalized. Proof: open -- no attempt yet.*
<!-- status:end -->

::: {.panel-tabset}

## Statement

[View PDF](pdf/main.pdf) · [Download LaTeX source](latex/main.tex) -- states both the secret-seed and public-seed variants (Conjectures 1 and 2 respectively); the public-seed case is tracked separately as [c/0004](/c/0004/index.qmd)

### Setting

Fix nonempty finite sets $\mathcal{K}$ (seeds), $\mathcal{D}$ (inputs), $\mathcal{R}$ (outputs), and write $K := |\mathcal{K}|$, $D := |\mathcal{D}|$, $R := |\mathcal{R}|$. Let $\mathsf{Fun}(\mathcal{K}\times\mathcal{D},\mathcal{R})$ be the set of all functions $\mathcal{K}\times\mathcal{D}\to\mathcal{R}$, let $\mathrm{SD}(\cdot,\cdot)$ denote statistical distance, and $U_\mathcal{R}$ the uniform distribution on $\mathcal{R}$. The source $S$, the predictor $P$, and the distinguisher $\mathsf{D}$ below are all computationally unbounded and receive the *entire* function table of $H$ as an explicit input.

```{=html}
<table class="cj-game-table">
<tbody>
<tr>
<th>Game \(\mathsf{Pred}^{S}_{P}\)</th>
<th>Game \(\mathsf{Ext}^{S}_{\mathsf{D}}\) (secret seed)</th>
</tr>
<tr>
<td>
\(H \stackrel{\$}{\leftarrow} \mathsf{Fun}(\mathcal{K}\times\mathcal{D},\mathcal{R})\)<br>
\((x,z) \stackrel{\$}{\leftarrow} S(H)\)<br>
\(x' \stackrel{\$}{\leftarrow} P(H,z)\)<br>
<b>return</b> \((x = x')\)
</td>
<td>
\(H \stackrel{\$}{\leftarrow} \mathsf{Fun}(\mathcal{K}\times\mathcal{D},\mathcal{R})\)<br>
\((x,z) \stackrel{\$}{\leftarrow} S(H)\); &nbsp; \(\mathit{sd} \stackrel{\$}{\leftarrow} \mathcal{K}\)<br>
\(y_0 \leftarrow H(\mathit{sd},x)\); &nbsp; \(y_1 \stackrel{\$}{\leftarrow} \mathcal{R}\)<br>
\(b \stackrel{\$}{\leftarrow} \{0,1\}\)<br>
\(b' \stackrel{\$}{\leftarrow} \mathsf{D}(H, y_b, z)\)<br>
<b>return</b> \((b = b')\)
</td>
</tr>
</tbody>
</table>
```

Prediction game (left) and the secret-seed extraction game (right). The seed $\mathit{sd}$ is still drawn, and still selects the row of $H$ that produces the real challenge, but $\mathsf{D}$ never receives it. That single omission is the only difference from the public-seed game of [c/0004](/c/0004/index.qmd).

::: {.callout-note title="Definition (Advantages and Unpredictability)"}
Set $\mathbf{Adv}^{\mathrm{pred}}_{\mathcal{K},\mathcal{D},\mathcal{R},S}(P) := \Pr[\mathsf{Pred}^{S}_{P} \Rightarrow 1]$ and
$$
\mathbf{Adv}^{\mathrm{ext}}_{\mathcal{K},\mathcal{D},\mathcal{R}}(S,\mathsf{D}) := 2\Pr[\mathsf{Ext}^{S}_{\mathsf{D}} \Rightarrow 1] - 1 .
$$
A source $S$ is *$\epsilon$-unpredictable* if $\mathbf{Adv}^{\mathrm{pred}}_{\mathcal{K},\mathcal{D},\mathcal{R},S}(P) \le \epsilon$ for every unbounded predictor $P$.
:::

### The Conjecture

::: {.callout-important title="Conjecture (Secret seed)"}
There is a universal constant $c > 0$, independent of $\mathcal{K}$, $\mathcal{D}$, $\mathcal{R}$ and $\epsilon$, such that for all nonempty finite $\mathcal{K}, \mathcal{D}, \mathcal{R}$, all $\epsilon \in (0,1]$, every $\epsilon$-unpredictable source $S$, and every unbounded distinguisher $\mathsf{D}$,
$$
\mathbf{Adv}^{\mathrm{ext}}_{\mathcal{K},\mathcal{D},\mathcal{R}}(S,\mathsf{D})
\;\le\; \delta(\epsilon, K, D, R)
\;:=\; c \cdot \sqrt{\frac{\epsilon R + \log_2 D}{K}} \; .
$$
:::

### Why this is not c/0004

Both summands sit under one square root here, and the whole expression carries the $1/K$. That is what a secret seed buys: the seed is $\log_2 K$ bits of entropy the distinguisher does not have, so it can help against the entropy-deficiency term as well as against the adversarial-support term, and both decay as the seed space grows.

Publishing the seed breaks exactly that. The proof attached to [c/0004](/c/0004/index.qmd) shows the transposed bound $c\sqrt{(\epsilon R + \log_2 D)/K}$ is **false** once $\mathsf{D}$ receives $\mathit{sd}$: with the seed in hand $\mathsf{D}$ can compute the whole row $x \mapsto H(\mathit{sd},x)$ and test image membership, an attack that does not weaken as $K$ grows. So the public-seed bound needs a $\sqrt{\epsilon R}$ term standing outside the $1/K$, and the two conjectures are genuinely different claims rather than restatements.

## Proof

No proof attempt is recorded here.

The secret-seed case is the one the [public-seed proof](/c/0004/index.qmd) takes as its foil rather than its input: that note proves the public-seed bound and disproves the naive transposition of this one, but it does not prove this one. Whether the four-step argument there (flatten the source to a fixed-size support, compute the mean statistical distance, control the deviation uniformly by a bounded-differences inequality, average) adapts to the secret-seed game, and whether it gives the combined-square-root shape above, is untested.

## Formal Artifact

Not yet formalized in Lean, neither the statement nor the proof.

The Lean statement for [c/0004](/c/0004/index.qmd) is close to reusable: its `Source`, `Predictor` and games are the same objects, and the secret-seed game is that file's `extGame` with the seed argument dropped from the distinguisher. Sharing those definitions across the two statements, rather than copying them, is the obvious first move and has not been made.

:::

## Open obligations

- [ ] Confirm the transcription against `latex/main.tex` Conjecture 1. This page was transcribed by an AI; `statement_informal` is `ai` for that reason, and may be raised to `human` by whoever wrote the source document if they judge it faithful.
- [ ] Attempt the proof, or record why the c/0004 argument does not carry over.
- [ ] Formalize the statement in Lean, sharing the game definitions with `c/0004/lean/` rather than duplicating them.
- [ ] Settle whether the combined-square-root shape is tight, or only an upper bound nobody has matched.
